# Infra paso 0 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dejar el VPS con separación estructural entre dev y producción — swap, Docker, y tres stacks Compose aislados corriendo una app Next.js mínima con healthcheck de contenido real — todo verificado por un script re-ejecutable.

**Architecture:** Un repo con la app y los tres compose files. `ngf-dev` monta el código con hot reload; `ngf-stage` corre la imagen buildeada contra un Postgres efímero en tmpfs; `ngf-prod` corre la misma imagen desde `/srv/negociofacil/prod/` sin código fuente. El aislamiento sale de usar proyectos Compose distintos (red y volúmenes prefijados por proyecto) y el reparto de recursos de dar a prod peso alto sin cap y a dev/stage cap duro.

**Tech Stack:** Next.js 16 (App Router) + React 19 + TypeScript, `pg` 8 para Postgres, Vitest 4 para tests, Docker Compose v2, Postgres 17, Caddy 2, Node 24.

**Spec:** `docs/superpowers/specs/2026-08-03-infra-paso-0-design.md`

## Global Constraints

- Imágenes base exactas: `node:24-alpine`, `postgres:17-alpine`, `caddy:2-alpine`.
- IP de Tailscale del servidor: `100.64.81.63`. IP pública: `178.156.251.41`.
- Límites de recursos con las claves de Compose **no-swarm**: `cpus`, `cpu_shares`, `mem_limit`, `oom_score_adj`. **Nunca** `deploy.resources`, que sin swarm se ignora en silencio.
- Los puertos de `ngf-dev` y `ngf-stage` se publican **atados a `100.64.81.63`**, nunca a `0.0.0.0`. Docker escribe reglas de iptables que se saltean ufw.
- Todo comentario, mensaje de commit y texto de UI en español.
- Ningún comando `docker compose down -v` sin `-p` explícito.
- Producción nunca corre desde `/root/negociofacil`.
- Presupuesto de recursos (de la spec, valores exactos):

  | Contenedor | CPU | Memoria |
  |---|---|---|
  | prod app | `cpu_shares: 1024`, sin cap | `mem_limit: 1536m` |
  | prod postgres | `cpu_shares: 1024`, sin cap, `oom_score_adj: -500` | `mem_limit: 1536m` |
  | prod caddy | `cpu_shares: 1024`, sin cap | `mem_limit: 128m` |
  | dev app | `cpus: 0.75`, `cpu_shares: 256` | `mem_limit: 1536m` |
  | dev postgres | `cpus: 0.25`, `cpu_shares: 256` | `mem_limit: 768m` |
  | stage app | `cpus: 0.5`, `cpu_shares: 128` | `mem_limit: 768m` |
  | stage postgres | `cpus: 0.25`, `cpu_shares: 128` | `mem_limit: 512m` |

---

## Estructura de archivos

| Archivo | Responsabilidad |
|---|---|
| `.gitignore` | Excluir `node_modules`, `.next`, `.env*` (menos `.env.example`) |
| `scripts/verify-infra.sh` | Verificación re-ejecutable de toda la infra. Crece suite por suite. |
| `scripts/setup-host.sh` | Provisioning idempotente del host: swap y Docker |
| `lib/health/types.ts` | Tipos del healthcheck |
| `lib/health/runChecks.ts` | Ejecución con timeout y agregación. Lógica pura, testeable sin red. |
| `lib/health/checks.ts` | La lista de checks concretos. **Punto de extensión.** |
| `lib/db.ts` | Pool de Postgres, singleton |
| `app/api/health/route.ts` | Endpoint HTTP: traduce el reporte a 200/503 |
| `app/page.tsx` | Muestra stack y SHA — verificación humana |
| `Dockerfile` | Build multi-stage a runtime no-root |
| `docker/compose.dev.yml` | Stack `ngf-dev` |
| `docker/compose.stage.yml` | Stack `ngf-stage` |
| `docker/compose.prod.yml` | Stack `ngf-prod` |
| `docker/Caddyfile` | Reverse proxy de prod |
| `.env.example` | Plantilla versionada de variables |

`lib/health/` va separado del route handler a propósito: la lógica de timeout y agregación es lo que más se va a testear y lo que va a crecer con los checks de tenant y pg-boss, y no debe requerir levantar un servidor HTTP para probarse.

---

## Task 1: Higiene del repo y esqueleto de verificación

**Files:**
- Create: `.gitignore`
- Create: `scripts/verify-infra.sh`

**Interfaces:**
- Consumes: nada.
- Produce: `scripts/verify-infra.sh [suite]` donde `suite` ∈ `host|app|network|limits|isolation|logs|all` (default `all`). Helpers de bash `ok`, `bad`, `check_cmd`, `suite_header` que las tareas siguientes reutilizan. Exit 0 si todo pasa, 1 si algo falla.

- [ ] **Step 1: Crear `.gitignore`**

```gitignore
node_modules/
.next/
out/
*.tsbuildinfo

.env
.env.*
!.env.example

npm-debug.log*
```

- [ ] **Step 2: Escribir `scripts/verify-infra.sh` con el harness y la suite `host`**

```bash
#!/usr/bin/env bash
# Verificación re-ejecutable de la infraestructura.
# CLAUDE.md: los límites son la única defensa entre un build y un cliente
# caído, así que comprobar que siguen puestos es parte del checklist de
# deploy — no algo que se configura una vez y se olvida.
set -uo pipefail

readonly TS_IP=100.64.81.63
readonly PUBLIC_IP=178.156.251.41

PASS=0
FAIL=0

suite_header() { printf '\n\033[1m%s\033[0m\n' "$1"; }
ok()  { printf '  \033[32m✓\033[0m %s\n' "$1"; PASS=$((PASS + 1)); }
bad() { printf '  \033[31m✗\033[0m %s\n' "$1"; FAIL=$((FAIL + 1)); }

# check_cmd "descripción" comando...  → pasa si el comando sale con 0
check_cmd() {
  local desc="$1"; shift
  if "$@" >/dev/null 2>&1; then ok "$desc"; else bad "$desc"; fi
}

# check_eq "descripción" esperado obtenido
check_eq() {
  local desc="$1" expected="$2" actual="$3"
  if [[ "$expected" == "$actual" ]]; then
    ok "$desc"
  else
    bad "$desc (esperado: $expected, obtenido: $actual)"
  fi
}

# check_ge "descripción" mínimo obtenido
check_ge() {
  local desc="$1" minimum="$2" actual="$3"
  if [[ "$actual" -ge "$minimum" ]]; then
    ok "$desc"
  else
    bad "$desc (mínimo: $minimum, obtenido: $actual)"
  fi
}

suite_host() {
  suite_header "Host: swap y Docker"

  local swap_mb
  swap_mb=$(free -m | awk '/^Swap:/ {print $2}')
  check_ge "swap de al menos 4 GB" 4000 "${swap_mb:-0}"

  check_eq "vm.swappiness en 10" "10" "$(sysctl -n vm.swappiness 2>/dev/null || echo NA)"
  check_cmd "swap persistida en /etc/fstab" grep -q '^/swapfile' /etc/fstab

  check_cmd "docker instalado" command -v docker
  check_cmd "plugin docker compose disponible" docker compose version

  local log_max live_restore
  # Acceso a campo escalar, no `index`: `index` necesita una colección más
  # una clave, y sobre un escalar es un error de template que dejaría este
  # check en NA para siempre.
  log_max=$(docker info --format '{{.LoggingDriver}}' 2>/dev/null || echo NA)
  check_eq "log driver json-file" "json-file" "$log_max"

  check_cmd "daemon.json con rotación de logs" \
    grep -q '"max-size"' /etc/docker/daemon.json
  live_restore=$(docker info --format '{{.LiveRestoreEnabled}}' 2>/dev/null || echo NA)
  check_eq "live-restore activado" "true" "$live_restore"
}

main() {
  local target="${1:-all}"
  case "$target" in
    host) suite_host ;;
    all)  suite_host ;;
    *) echo "suite desconocida: $target" >&2; exit 2 ;;
  esac

  printf '\n%s: \033[32m%d ok\033[0m, \033[31m%d fallan\033[0m\n' "$target" "$PASS" "$FAIL"
  [[ "$FAIL" -eq 0 ]]
}

main "$@"
```

- [ ] **Step 3: Hacerlo ejecutable y correrlo para verificar que falla**

Run: `chmod +x scripts/verify-infra.sh && ./scripts/verify-infra.sh host; echo "exit=$?"`
Expected: FAIL — swap 0 MB, docker ausente. Debe imprimir varios `✗` y salir con `exit=1`. Si sale 0, el script está mintiendo y hay que arreglarlo antes de seguir.

- [ ] **Step 4: Versionar los archivos que estaban sueltos**

`CLAUDE.md` es la fuente de verdad del proyecto y hoy sólo existe en el disco del VPS, que el propio documento describe como punto único de falla.

```bash
git add .gitignore scripts/verify-infra.sh CLAUDE.md package.json package-lock.json .mcp.json
git status --short   # node_modules/ ya no debe aparecer
```

- [ ] **Step 5: Commit**

```bash
git commit -m "chore: gitignore, script de verificacion y versionado de CLAUDE.md

El harness de verify-infra.sh arranca fallando: no hay swap ni Docker.
Las tareas siguientes lo van poniendo en verde suite por suite."
```

---

## Task 2: Swap

**Files:**
- Create: `scripts/setup-host.sh`

**Interfaces:**
- Consumes: `scripts/verify-infra.sh host` de la Task 1.
- Produce: `scripts/setup-host.sh`, idempotente, corre como root. Después de esta tarea configura swap; la Task 3 le agrega Docker.

- [ ] **Step 1: Escribir `scripts/setup-host.sh` con la sección de swap**

```bash
#!/usr/bin/env bash
# Provisioning del host. Idempotente: correrlo dos veces no rompe nada.
# Se versiona para que la máquina sea reproducible — si el VPS se pierde,
# esto es lo que lo reconstruye.
set -euo pipefail

[[ $EUID -eq 0 ]] || { echo "Correr como root" >&2; exit 1; }

readonly SWAPFILE=/swapfile
readonly SWAP_SIZE=4G

setup_swap() {
  if swapon --show --noheadings | grep -q "$SWAPFILE"; then
    echo "swap ya activa, salteando"
  else
    echo "creando swapfile de $SWAP_SIZE"
    fallocate -l "$SWAP_SIZE" "$SWAPFILE"
    chmod 600 "$SWAPFILE"
    mkswap "$SWAPFILE"
    swapon "$SWAPFILE"
  fi

  # Persistir el montaje
  grep -q "^$SWAPFILE" /etc/fstab || echo "$SWAPFILE none swap sw 0 0" >> /etc/fstab

  # swappiness bajo a propósito: la swap está para absorber el pico de un
  # build, no para que el kernel pagine Postgres de forma proactiva.
  echo 'vm.swappiness=10' > /etc/sysctl.d/99-ngf-swappiness.conf
  sysctl -q -w vm.swappiness=10
}

setup_swap
echo "listo"
```

- [ ] **Step 2: Correrlo**

Run: `chmod +x scripts/setup-host.sh && sudo ./scripts/setup-host.sh`
Expected: crea el swapfile y termina con "listo".

- [ ] **Step 3: Verificar que es idempotente**

Run: `sudo ./scripts/setup-host.sh`
Expected: imprime "swap ya activa, salteando" y termina en 0. Sin esto, el script no sirve para reconstruir la máquina.

- [ ] **Step 4: Correr la verificación**

Run: `./scripts/verify-infra.sh host; echo "exit=$?"`
Expected: los tres checks de swap en `✓`; los de Docker siguen en `✗` y `exit=1`.

- [ ] **Step 5: Commit**

```bash
git add scripts/setup-host.sh
git commit -m "feat: swap de 4GB con swappiness 10

Sin swap, un OOM durante un build deja que el kernel elija victima,
y puede elegir el Postgres de produccion."
```

---

## Task 3: Docker y configuración del daemon

**Files:**
- Modify: `scripts/setup-host.sh` (agregar `setup_docker`)

**Interfaces:**
- Consumes: `scripts/setup-host.sh` de la Task 2.
- Produce: Docker instalado con rotación de logs global y `live-restore`. La suite `host` queda entera en verde.

- [ ] **Step 1: Agregar `setup_docker` a `scripts/setup-host.sh`**

Insertar esta función después de `setup_swap` y agregar `setup_docker` a las llamadas al final del archivo (antes de `echo "listo"`).

```bash
setup_docker() {
  if command -v docker >/dev/null 2>&1; then
    echo "docker ya instalado, salteando instalación"
  else
    echo "instalando docker desde el repositorio oficial"
    apt-get update -qq
    apt-get install -y -qq ca-certificates curl
    install -m 0755 -d /etc/apt/keyrings
    curl -fsSL https://download.docker.com/linux/ubuntu/gpg \
      -o /etc/apt/keyrings/docker.asc
    chmod a+r /etc/apt/keyrings/docker.asc
    echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}") stable" \
      > /etc/apt/sources.list.d/docker.list
    apt-get update -qq
    apt-get install -y -qq docker-ce docker-ce-cli containerd.io \
      docker-buildx-plugin docker-compose-plugin
  fi

  # Rotación a nivel daemon, no sólo en cada compose: un contenedor
  # levantado a mano tampoco puede llenar el disco.
  # live-restore: reiniciar el daemon no tira los contenedores de prod.
  cat > /etc/docker/daemon.json <<'JSON'
{
  "log-driver": "json-file",
  "log-opts": {
    "max-size": "10m",
    "max-file": "3"
  },
  "live-restore": true
}
JSON

  systemctl enable --now docker
  systemctl restart docker
}
```

**Enmienda (aplicada durante la ejecución).** Las dos últimas líneas como están escritas rompen la idempotencia que este mismo script exige: reinician el daemon en cada corrida. `live-restore` no cubre ese caso — lo decide la configuración que cargó el daemon **que se está muriendo**, no la que va a leer el que arranca, así que justo el reinicio que activa `live-restore` por primera vez no está protegido y frena todo lo que esté corriendo. En una máquina vacía es inocuo; en la receta de reconstrucción de un host con clientes, no.

La versión implementada escribe el `daemon.json` deseado a un temporal, lo compara con el existente (`cmp -s`), y sólo instala y reinicia si difieren. `systemctl enable --now docker` queda reservado a la rama de instalación real, con un flag `recien_instalado`. Ver `scripts/setup-host.sh` para el código vigente.

- [ ] **Step 2: Correrlo**

Run: `sudo ./scripts/setup-host.sh`
Expected: instala Docker y termina con "listo".

Nota: si el codename de Ubuntu 26.04 todavía no tiene repo en download.docker.com, el `apt-get install` falla. En ese caso usar el codename LTS anterior en la línea del repo y anotarlo como comentario en el script — es la desviación esperable y no debe resolverse instalando `docker.io` de Ubuntu, que trae una versión de Compose distinta.

- [ ] **Step 3: Verificar idempotencia**

Run: `sudo ./scripts/setup-host.sh`
Expected: "docker ya instalado, salteando instalación", exit 0.

- [ ] **Step 4: Verificar que los límites de memoria funcionan de verdad**

El kernel puede no exponer cgroups de memoria; mejor descubrirlo ahora que cuando haya un cliente.

Run: `docker run --rm --memory=64m alpine sh -c 'cat /sys/fs/cgroup/memory.max'`
Expected: `67108864`. Si dice `max`, los límites no aplican y hay que resolverlo antes de seguir.

- [ ] **Step 5: Correr la verificación**

Run: `./scripts/verify-infra.sh host; echo "exit=$?"`
Expected: suite `host` entera en verde, `exit=0`.

- [ ] **Step 6: Commit**

```bash
git add scripts/setup-host.sh
git commit -m "feat: docker con rotacion de logs global y live-restore

Rotacion a nivel daemon para que un contenedor levantado a mano
tampoco pueda llenar el disco."
```

---

## Task 4: Esqueleto Next.js y página de identificación

**Files:**
- Create: `package.json`, `tsconfig.json`, `next.config.ts`, `app/layout.tsx`, `app/page.tsx`, `vitest.config.ts` (los genera `create-next-app`, después se ajustan)
- Create: `.env.example`

**Interfaces:**
- Consumes: nada de tareas anteriores.
- Produce: app Next.js que arranca con `npm run dev`; `npm test` corre Vitest; `next.config.ts` con `output: 'standalone'`; variables de entorno `NGF_STACK` y `GIT_SHA` leídas por `app/page.tsx`.

- [ ] **Step 1: Generar el esqueleto**

`create-next-app` resuelve versiones coherentes de Next, React y TypeScript entre sí — por eso se usa en vez de fijarlas a mano.

**Verificado empíricamente:** `create-next-app` aborta si el directorio contiene `package.json`, `CLAUDE.md` o `scripts/`. Permite `.git`, `.gitignore`, `README.md` y `docs/`. Hay que apartarlos y devolverlos.

`package.json` y `package-lock.json` hoy sólo declaran `shadcn`, que es para trabajo de UI posterior; se re-agrega al final del paso.

```bash
cd /root/negociofacil

# Apartar lo que hace abortar al scaffolder. /tmp no: si el paso se
# interrumpe, el material queda donde se lo encuentra.
mkdir -p .scaffold-hold
mv CLAUDE.md scripts .superpowers .scaffold-hold/
rm -rf package.json package-lock.json node_modules

npx create-next-app@latest . --typescript --app --eslint \
  --no-tailwind --no-src-dir --import-alias "@/*" --use-npm

# Devolver todo y re-agregar shadcn
mv .scaffold-hold/CLAUDE.md .scaffold-hold/scripts .scaffold-hold/.superpowers .
rmdir .scaffold-hold
npm install -D shadcn
```

- [ ] **Step 1b: Verificar que no se perdió nada versionado**

Este paso movió y borró archivos que están en git. Confirmarlo antes de seguir.

```bash
git status --short
git diff --stat HEAD -- CLAUDE.md scripts/
```
Expected: `CLAUDE.md` y `scripts/` **no** aparecen como borrados ni modificados. Si aparecen, restaurarlos con `git checkout HEAD -- CLAUDE.md scripts/` antes de continuar.

- [ ] **Step 2: Instalar `pg` y Vitest**

```bash
npm install pg
npm install -D vitest @types/pg
```

- [ ] **Step 3: Configurar `output: 'standalone'` en `next.config.ts`**

Sin esto el Dockerfile de la Task 7 no tiene qué copiar.

```typescript
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Necesario para la imagen Docker: genera .next/standalone con un
  // server.js autocontenido y sólo las dependencias que se usan.
  output: 'standalone',
}

export default nextConfig
```

- [ ] **Step 4: Crear `vitest.config.mts`**

La extensión `.mts` no es un detalle: con `.ts`, Vite carga el archivo como CommonJS y emite un warning en **cada** corrida de tests — incluida la que `deploy.sh` usa como gate. Ruido en la salida de un gate es cómo se termina pasando por alto un warning que sí importa. Y como `.mts` es ESM, `__dirname` no existe: va `import.meta.dirname` (Node ≥ 20.11).

```typescript
import { defineConfig } from 'vitest/config'
import path from 'node:path'

export default defineConfig({
  test: {
    environment: 'node',
    include: ['lib/**/*.test.ts'],
  },
  resolve: {
    alias: { '@': path.resolve(import.meta.dirname, '.') },
  },
})
```

- [ ] **Step 5: Agregar el script de test a `package.json`**

En `"scripts"`, agregar: `"test": "vitest run"`

- [ ] **Step 6: Escribir `app/page.tsx`**

```tsx
// Verificación humana más barata que existe: saber de un vistazo qué
// imagen está corriendo en qué stack.
export const dynamic = 'force-dynamic'

export default function Home() {
  const stack = process.env.NGF_STACK ?? 'desconocido'
  const sha = process.env.GIT_SHA ?? 'dev'

  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', padding: '3rem' }}>
      <h1>NegocioFácil</h1>
      <dl>
        <dt>Stack</dt>
        <dd data-testid="stack">{stack}</dd>
        <dt>Imagen</dt>
        <dd data-testid="sha">{sha}</dd>
      </dl>
    </main>
  )
}
```

- [ ] **Step 6b: Limpiar `app/layout.tsx`** (enmienda aplicada durante la ejecución)

El scaffolder deja dos cosas que no sirven acá. Ponerlo en español es la constraint global; sacar las fuentes es lo que importa de verdad.

- `lang="es"`, `title: "NegocioFácil"` y una descripción en español.
- **Eliminar el import de `next/font/google` y las dos declaraciones de fuentes.** Se cargan pero nunca se renderizan — `globals.css` fija Arial y `page.tsx` inlinea `system-ui` encima. Y `next/font/google` hace un fetch al CDN de Google **en tiempo de build**, que es dentro de la imagen Docker, en cada deploy. Es un modo de falla de deploy a cambio de nada.

- [ ] **Step 7: Crear `.env.example`**

```bash
# Copiar a .env.dev (nunca commitear el .env real).
# Las credenciales de cada stack son distintas a propósito: evita que una
# cadena de conexión copiada por error apunte a la base equivocada.
POSTGRES_USER=ngf_dev
POSTGRES_PASSWORD=cambiar-en-cada-stack
POSTGRES_DB=ngf_dev
DATABASE_URL=postgres://ngf_dev:cambiar-en-cada-stack@postgres:5432/ngf_dev
NGF_STACK=dev
```

- [ ] **Step 8: Verificar que arranca y que los tests corren**

```bash
npm run build
npm test
```
Expected: el build termina y genera `.next/standalone`. `npm test` sale 0 diciendo que no encontró tests (todavía no hay ninguno) — confirma que Vitest está bien configurado.

- [ ] **Step 9: Commit**

```bash
git add -A
git commit -m "feat: esqueleto Next.js con build standalone y pagina de identificacion"
```

---

## Task 5: Lógica del healthcheck

**Files:**
- Create: `lib/health/types.ts`
- Create: `lib/health/runChecks.ts`
- Test: `lib/health/runChecks.test.ts`

**Interfaces:**
- Consumes: Vitest configurado en la Task 4.
- Produce:
  - `type HealthCheck = { name: string; timeoutMs: number; run: () => Promise<string | void> }`
  - `type CheckResult = { name: string; ok: boolean; durationMs: number; detail?: string }`
  - `type HealthReport = { status: 'ok' | 'degraded'; checks: CheckResult[] }`
  - `runChecks(checks: HealthCheck[]): Promise<HealthReport>`

- [ ] **Step 1: Escribir el test que falla**

`lib/health/runChecks.test.ts`:

```typescript
import { describe, it, expect } from 'vitest'
import { runChecks } from '@/lib/health/runChecks'
import type { HealthCheck } from '@/lib/health/types'

const check = (
  name: string,
  run: HealthCheck['run'],
  timeoutMs = 50,
): HealthCheck => ({ name, timeoutMs, run })

describe('runChecks', () => {
  it('reporta ok cuando todos los checks pasan', async () => {
    const report = await runChecks([
      check('a', async () => {}),
      check('b', async () => 'sha=abc123'),
    ])

    expect(report.status).toBe('ok')
    expect(report.checks.map((c) => c.name)).toEqual(['a', 'b'])
    expect(report.checks[1].detail).toBe('sha=abc123')
  })

  it('reporta degraded y el motivo cuando un check lanza', async () => {
    const report = await runChecks([
      check('postgres', async () => {
        throw new Error('conexión rechazada')
      }),
    ])

    expect(report.status).toBe('degraded')
    expect(report.checks[0].ok).toBe(false)
    expect(report.checks[0].detail).toBe('conexión rechazada')
  })

  it('corta un check colgado en su timeout en vez de esperarlo', async () => {
    const report = await runChecks([
      check('colgado', () => new Promise(() => {}), 20),
    ])

    expect(report.checks[0].ok).toBe(false)
    expect(report.checks[0].detail).toMatch(/timeout/)
  })

  it('aísla la falla: un check roto no impide reportar los demás', async () => {
    const report = await runChecks([
      check('roto', async () => {
        throw new Error('boom')
      }),
      check('sano', async () => {}),
    ])

    expect(report.status).toBe('degraded')
    expect(report.checks).toHaveLength(2)
    expect(report.checks[1].ok).toBe(true)
  })
})
```

- [ ] **Step 2: Correr el test y verificar que falla**

Run: `npm test`
Expected: FAIL — no puede resolver `@/lib/health/runChecks`.

- [ ] **Step 3: Escribir `lib/health/types.ts`**

```typescript
/** Un check individual del healthcheck. Agregar uno es sumar un elemento
 *  a la lista de `lib/health/checks.ts` — es el punto de extensión. */
export type HealthCheck = {
  name: string
  timeoutMs: number
  /** Devuelve un detalle opcional para el reporte; lanza si el check falla. */
  run: () => Promise<string | void>
}

export type CheckResult = {
  name: string
  ok: boolean
  durationMs: number
  detail?: string
}

export type HealthReport = {
  status: 'ok' | 'degraded'
  checks: CheckResult[]
}
```

- [ ] **Step 4: Escribir `lib/health/runChecks.ts`**

```typescript
import type { HealthCheck, CheckResult, HealthReport } from './types'

async function withTimeout(
  promise: Promise<string | void>,
  ms: number,
): Promise<string | void> {
  let timer: ReturnType<typeof setTimeout> | undefined
  const timeout = new Promise<never>((_, reject) => {
    timer = setTimeout(() => reject(new Error(`timeout tras ${ms}ms`)), ms)
  })
  try {
    return await Promise.race([promise, timeout])
  } finally {
    clearTimeout(timer)
  }
}

async function runOne(check: HealthCheck): Promise<CheckResult> {
  const started = performance.now()
  try {
    const detail = await withTimeout(check.run(), check.timeoutMs)
    return {
      name: check.name,
      ok: true,
      durationMs: Math.round(performance.now() - started),
      ...(detail ? { detail } : {}),
    }
  } catch (err) {
    return {
      name: check.name,
      ok: false,
      durationMs: Math.round(performance.now() - started),
      detail: err instanceof Error ? err.message : String(err),
    }
  }
}

/** Corre todos los checks en paralelo. Cada uno falla aislado: que
 *  Postgres no responda no impide reportar el estado de los demás. */
export async function runChecks(checks: HealthCheck[]): Promise<HealthReport> {
  const results = await Promise.all(checks.map(runOne))
  return {
    status: results.every((r) => r.ok) ? 'ok' : 'degraded',
    checks: results,
  }
}
```

- [ ] **Step 5: Correr los tests**

Run: `npm test`
Expected: PASS, 4 tests.

**Enmienda (aplicada durante la ejecución): `AggregateError` deja el `detail` vacío.** Cuando Node intenta conectar por varias vías (`::1` y `127.0.0.1`), rechaza con un `AggregateError` cuyo `.message` es `""` — los mensajes reales viven en `.errors[]`. Como `AggregateError` **es** un `Error`, la línea `err instanceof Error ? err.message : String(err)` produce un detalle vacío: el healthcheck dice que Postgres falló pero no dice por qué, justo en su modo de falla más probable.

La versión implementada extrae una función `detailFromError` que aplana `AggregateError` en sus submensajes unidos con `; `, deja `.message` para los `Error` comunes y `String(err)` para el resto. Va acá y no en el check de Postgres a propósito: `AggregateError` es un concepto de Node, no de pg, y los checks que faltan (tenant, pg-boss) se toparían con lo mismo. Hay un quinto test que lo fija.

- [ ] **Step 6: Commit**

```bash
git add lib/health package.json
git commit -m "feat: logica del healthcheck con timeout y fallas aisladas

La lista de checks es el punto de extension: sumar tenant y pg-boss
mas adelante es agregar entradas, no reescribir el endpoint."
```

---

## Task 6: Endpoint `/api/health` y pool de Postgres

**Files:**
- Create: `lib/db.ts`
- Create: `lib/health/checks.ts`
- Create: `app/api/health/route.ts`

**Interfaces:**
- Consumes: `runChecks`, `HealthCheck` de la Task 5.
- Produce: `GET /api/health` → 200 con `HealthReport` si todo pasa, 503 si algo falla. `export const checks: HealthCheck[]` en `lib/health/checks.ts`.

- [ ] **Step 1: Escribir `lib/db.ts`**

```typescript
import { Pool } from 'pg'

// Singleton: en dev el hot reload re-evalúa los módulos y sin esto se
// abriría un pool nuevo en cada recarga hasta agotar las conexiones.
const globalForPg = globalThis as unknown as { ngfPool?: Pool }

export const pool =
  globalForPg.ngfPool ??
  new Pool({
    connectionString: process.env.DATABASE_URL,
    max: 5,
    connectionTimeoutMillis: 2000,
  })

if (process.env.NODE_ENV !== 'production') globalForPg.ngfPool = pool
```

**Enmienda (aplicada durante la ejecución): falta el listener de `'error'` del pool, y su ausencia tira abajo el proceso.** `pg-pool` reemite como evento `'error'` del Pool los errores de red de un cliente **ocioso** — la conexión TCP se muere porque Postgres reinició o hubo un corte breve. Un `EventEmitter` sin listener para `'error'` **lanza** esa excepción sin capturar, y eso mata el proceso de Next.js entero, para todos los tenants, por un cliente que ni siquiera estaba en uso.

La versión implementada extrae la construcción a una función `crearPool()` que atacha `p.on('error', ...)` con logueo a stderr antes de devolver el pool, y mantiene el singleton. Lleva un comentario largo explicando por qué existe, para que nadie lo borre por parecer código muerto.

- [ ] **Step 2: Escribir `lib/health/checks.ts`**

```typescript
import { pool } from '@/lib/db'
import type { HealthCheck } from './types'

const appCheck: HealthCheck = {
  name: 'app',
  timeoutMs: 1000,
  // Devolver SHA y uptime distingue "la app arrancó de verdad" de
  // "hay algo escuchando en el puerto".
  run: async () =>
    `sha=${process.env.GIT_SHA ?? 'dev'} uptime=${Math.round(process.uptime())}s`,
}

const postgresCheck: HealthCheck = {
  name: 'postgres',
  timeoutMs: 2000,
  run: async () => {
    const res = await pool.query('SELECT 1 AS ok')
    if (res.rows[0]?.ok !== 1) throw new Error('respuesta inesperada de Postgres')
  },
}

/**
 * La lista de checks del healthcheck.
 *
 * PENDIENTE — bloqueante antes del primer deploy real (ver CLAUDE.md):
 * falta el check de una query filtrada por tenant y el de pg-boss. Sin
 * ellos el rollback automático se dispara con criterio incompleto.
 */
export const checks: HealthCheck[] = [appCheck, postgresCheck]
```

- [ ] **Step 3: Escribir `app/api/health/route.ts`**

```typescript
import { NextResponse } from 'next/server'
import { runChecks } from '@/lib/health/runChecks'
import { checks } from '@/lib/health/checks'

// Un healthcheck cacheado es un healthcheck que miente.
export const dynamic = 'force-dynamic'

export async function GET() {
  const report = await runChecks(checks)
  return NextResponse.json(report, {
    status: report.status === 'ok' ? 200 : 503,
  })
}
```

- [ ] **Step 4: Verificar que devuelve 503 sin Postgres**

Todavía no hay base corriendo, así que el check de Postgres debe fallar. Que dé 503 acá es exactamente lo que se quiere: prueba que el endpoint no devuelve 200 por defecto.

```bash
npm run dev &
sleep 5
curl -s -o /tmp/health.json -w '%{http_code}\n' localhost:3000/api/health
cat /tmp/health.json
kill %1
```
Expected: `503`, y el JSON muestra `"name":"app","ok":true` junto a `"name":"postgres","ok":false`.

- [ ] **Step 5: Verificar que el build sigue pasando**

Run: `npm run build && npm test`
Expected: ambos en verde.

- [ ] **Step 6: Commit**

```bash
git add lib/db.ts lib/health/checks.ts app/api/health
git commit -m "feat: endpoint /api/health con checks de app y Postgres

Devuelve 503 identificando que check fallo, nunca 200 fijo."
```

---

## Task 7: Dockerfile

**Files:**
- Create: `Dockerfile`
- Create: `.dockerignore`

**Interfaces:**
- Consumes: `output: 'standalone'` de la Task 4.
- Produce: imagen `ngf-app:<tag>` que corre como usuario no-root, escucha en 3000, y acepta `--build-arg GIT_SHA=<sha>`.

- [ ] **Step 1: Escribir `.dockerignore`**

```
node_modules
.next
.git
.env
.env.*
docs
scripts
**/*.test.ts
```

- [ ] **Step 2: Escribir `Dockerfile`**

```dockerfile
# syntax=docker/dockerfile:1

FROM node:24-alpine AS deps
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci

FROM node:24-alpine AS build
WORKDIR /app
ENV NEXT_TELEMETRY_DISABLED=1
COPY --from=deps /app/node_modules ./node_modules
COPY . .
RUN npm run build

FROM node:24-alpine AS runtime
WORKDIR /app
ENV NODE_ENV=production \
    NEXT_TELEMETRY_DISABLED=1 \
    PORT=3000 \
    HOSTNAME=0.0.0.0

# Usuario no-root: si alguien escapa del proceso, no arranca como root.
RUN addgroup -g 1001 -S ngf && adduser -u 1001 -S ngf -G ngf

# Sólo el output standalone: sin código fuente ni devDependencies.
COPY --from=build --chown=ngf:ngf /app/.next/standalone ./
COPY --from=build --chown=ngf:ngf /app/.next/static ./.next/static
COPY --from=build --chown=ngf:ngf /app/public ./public

ARG GIT_SHA
# Sin default a propósito. Con `ARG GIT_SHA=dev`, un build sin el
# build-arg sale bien y la imagen después reporta `sha=dev` por el
# healthcheck aunque tenga código real adentro — y "dev" se lee como un
# valor legítimo, así que el error es invisible. Justo ese valor es el
# que alguien mira a las 11 de la noche para saber qué está corriendo.
# Una imagen mal etiquetada es peor que un build que falla.
RUN test -n "$GIT_SHA" || { \
      echo "ERROR: falta --build-arg GIT_SHA=\$(git rev-parse --short HEAD)."; \
      echo "El build no continúa sin saber qué código está empaquetando."; \
      exit 1; \
    }
ENV GIT_SHA=$GIT_SHA

USER ngf
EXPOSE 3000
CMD ["node", "server.js"]
```

- [ ] **Step 3: Buildear con CPU limitada**

Un build nunca compite de igual a igual con un cliente.

```bash
nice -n 15 docker build --cpuset-cpus=0 --memory=2g \
  --build-arg GIT_SHA="$(git rev-parse --short HEAD)" \
  -t "ngf-app:$(git rev-parse --short HEAD)" .
```
Expected: build exitoso.

- [ ] **Step 4: Verificar que la imagen corre como no-root y no trae el código fuente**

```bash
TAG=$(git rev-parse --short HEAD)
docker run --rm "ngf-app:$TAG" whoami          # esperado: ngf
docker run --rm "ngf-app:$TAG" ls /app/lib     # esperado: no existe
```
Expected: `ngf`, y el segundo comando falla con "No such file or directory". Si `lib/` está en la imagen, el `.dockerignore` no está haciendo su trabajo.

- [ ] **Step 5: Verificar que la imagen responde 503 sin base**

```bash
TAG=$(git rev-parse --short HEAD)
docker run --rm -d --name ngf-smoke -p 127.0.0.1:3999:3000 "ngf-app:$TAG"
sleep 3
curl -s -o /dev/null -w '%{http_code}\n' localhost:3999/api/health
docker rm -f ngf-smoke
```
Expected: `503`.

- [ ] **Step 6: Commit**

```bash
git add Dockerfile .dockerignore
git commit -m "feat: Dockerfile multi-stage con runtime no-root

La imagen final lleva solo el output standalone: sin codigo fuente
ni devDependencies."
```

---

## Task 8: Stack `ngf-dev`

**Files:**
- Create: `docker/compose.dev.yml`
- Create: `.env.dev` (no versionado)
- Modify: `scripts/verify-infra.sh` (suites `app` y `network`)

**Interfaces:**
- Consumes: `.env.example` de la Task 4, endpoint de la Task 6.
- Produce: stack `ngf-dev` con hot reload en `100.64.81.63:3000`. Suites `app` y `network` de `verify-infra.sh`.

- [ ] **Step 1: Escribir `docker/compose.dev.yml`**

```yaml
# Stack de desarrollo. Escucha SOLO en la IP de Tailscale: Docker escribe
# reglas de iptables que se saltean ufw, así que el bind explícito es la
# defensa real, no el firewall.
name: ngf-dev

services:
  postgres:
    image: postgres:17-alpine
    env_file: ../.env.dev
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "100.64.81.63:5433:5432"
    cpus: 0.25
    cpu_shares: 256
    mem_limit: 768m
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 3s
      retries: 10

  app:
    image: node:24-alpine
    working_dir: /app
    # Instala sólo si falta: npm ci en cada `up` costaría medio minuto.
    command: sh -c "[ -d node_modules/next ] || npm ci; npm run dev"
    env_file: ../.env.dev
    environment:
      NGF_STACK: dev
      HOSTNAME: 0.0.0.0
    volumes:
      - ../:/app
      - node_modules:/app/node_modules
    ports:
      - "100.64.81.63:3000:3000"
    cpus: 0.75
    cpu_shares: 256
    mem_limit: 1536m
    # Sin restart a propósito: en dev un crash tiene que verse, no
    # esconderse detrás de un reinicio automático.
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  pgdata:
  node_modules:
```

- [ ] **Step 2: Crear `.env.dev`**

```bash
cp .env.example .env.dev
sed -i "s/cambiar-en-cada-stack/$(openssl rand -hex 16)/g" .env.dev
chmod 600 .env.dev
grep -c cambiar-en-cada-stack .env.dev   # esperado: 0
```

- [ ] **Step 3: Levantar el stack**

```bash
docker compose -f docker/compose.dev.yml up -d
docker compose -p ngf-dev ps
```
Expected: `postgres` healthy y `app` corriendo.

- [ ] **Step 4: Verificar que ahora el healthcheck da 200**

```bash
sleep 20
curl -s localhost:3000/api/health | tee /dev/stderr | grep -q '"status":"ok"'
```
Expected: JSON con `"status":"ok"` y los dos checks en `true`. Es la primera vez que el check de Postgres pasa.

- [ ] **Step 5: Agregar las suites `app` y `network` a `scripts/verify-infra.sh`**

Insertar antes de `main()` y agregar los casos `app` y `network` al `case` (y llamarlas desde `all`, en el orden `suite_host; suite_app; suite_network`).

```bash
suite_app() {
  suite_header "App: healthcheck con contenido real"

  local code body
  code=$(curl -s -o /tmp/ngf-health.json -w '%{http_code}' \
    "http://$TS_IP:3000/api/health" 2>/dev/null || echo 000)
  check_eq "healthcheck de dev devuelve 200" "200" "$code"

  body=$(cat /tmp/ngf-health.json 2>/dev/null || echo '{}')
  check_cmd "reporta el check de app" grep -q '"name":"app"' /tmp/ngf-health.json
  check_cmd "reporta el check de postgres" \
    grep -q '"name":"postgres"' /tmp/ngf-health.json
  check_cmd "el check de app expone el SHA" grep -q 'sha=' /tmp/ngf-health.json
}

suite_network() {
  suite_header "Red: dev y stage sólo por Tailscale"

  # Chequeo 4 de la spec: el bind, no el firewall, es lo que protege.
  local bad_binds
  bad_binds=$(ss -ltn 2>/dev/null | awk '{print $4}' \
    | grep -E '^(0\.0\.0\.0|\*):(3000|3001|5433)$' | wc -l)
  check_eq "puertos de dev/stage no escuchan en 0.0.0.0" "0" "$bad_binds"

  check_cmd "puerto 3000 escucha en la IP de Tailscale" \
    bash -c "ss -ltn | grep -q '$TS_IP:3000'"

  # Desde la IP pública debe rechazar la conexión.
  if timeout 3 bash -c "</dev/tcp/$PUBLIC_IP/3000" 2>/dev/null; then
    bad "puerto 3000 alcanzable desde la IP pública"
  else
    ok "puerto 3000 no alcanzable desde la IP pública"
  fi
}
```

- [ ] **Step 6: Correr las suites nuevas**

Run: `./scripts/verify-infra.sh app && ./scripts/verify-infra.sh network`
Expected: ambas en verde.

- [ ] **Step 7: Commit**

```bash
git add docker/compose.dev.yml scripts/verify-infra.sh
git status --short   # .env.dev NO debe aparecer
git commit -m "feat: stack ngf-dev con hot reload atado a Tailscale"
```

---

## Task 9: Stack `ngf-prod`

**Files:**
- Create: `docker/compose.prod.yml`
- Create: `docker/Caddyfile`
- Modify: `scripts/verify-infra.sh` (suites `limits` e `isolation`)

**Interfaces:**
- Consumes: imagen `ngf-app:<tag>` de la Task 7.
- Produce: stack `ngf-prod` corriendo desde `/srv/negociofacil/prod/`, detrás de Caddy en 80/443. Suites `limits` e `isolation`.

- [ ] **Step 1: Escribir `docker/compose.prod.yml`**

Las rutas son relativas a `/srv/negociofacil/prod/`, no al repo.

```yaml
# Stack de producción. Corre desde /srv/negociofacil/prod/, sin código
# fuente: sólo este archivo, el .env, el Caddyfile y los volúmenes.
name: ngf-prod

services:
  postgres:
    image: postgres:17-alpine
    env_file: .env
    volumes:
      - pgdata:/var/lib/postgresql/data
    cpu_shares: 1024
    mem_limit: 1536m
    # Último candidato del OOM killer: si la máquina se queda sin
    # memoria, que muera cualquier otra cosa antes que la base.
    oom_score_adj: -500
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 3s
      retries: 5

  app:
    image: "ngf-app:${IMAGE_TAG:?IMAGE_TAG es obligatorio}"
    env_file: .env
    environment:
      NGF_STACK: prod
    # Sin cap de CPU: con cpu_shares alto gana bajo contención y aprovecha
    # los dos cores cuando dev está quieto.
    cpu_shares: 1024
    mem_limit: 1536m
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy

  caddy:
    image: caddy:2-alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddydata:/data
      - caddyconfig:/config
    cpu_shares: 1024
    mem_limit: 128m
    restart: unless-stopped
    depends_on:
      - app

volumes:
  pgdata:
  caddydata:
  caddyconfig:
```

- [ ] **Step 2: Escribir `docker/Caddyfile`**

```
# Certificado interno mientras negociofacil.com siga apuntando al parking
# de AWS. El wildcard por DNS-01 entra cuando se apunte el DNS.
:443 {
	tls internal
	reverse_proxy app:3000
}

:80 {
	reverse_proxy app:3000
}
```

- [ ] **Step 3: Preparar `/srv/negociofacil/prod/` con credenciales propias**

```bash
sudo mkdir -p /srv/negociofacil/prod
sudo cp docker/compose.prod.yml /srv/negociofacil/prod/docker-compose.yml
sudo cp docker/Caddyfile /srv/negociofacil/prod/Caddyfile

PROD_PASS=$(openssl rand -hex 16)
sudo tee /srv/negociofacil/prod/.env >/dev/null <<EOF
POSTGRES_USER=ngf_prod
POSTGRES_PASSWORD=$PROD_PASS
POSTGRES_DB=ngf_prod
DATABASE_URL=postgres://ngf_prod:$PROD_PASS@postgres:5432/ngf_prod
IMAGE_TAG=$(git rev-parse --short HEAD)
EOF
sudo chmod 600 /srv/negociofacil/prod/.env
```

- [ ] **Step 4: Confirmar que las credenciales de prod y dev son distintas**

```bash
sudo grep POSTGRES_PASSWORD /srv/negociofacil/prod/.env | md5sum
grep POSTGRES_PASSWORD .env.dev | md5sum
```
Expected: hashes distintos. Si coinciden, algo copió el `.env` en vez de generarlo.

- [ ] **Step 5: Levantar prod**

```bash
cd /srv/negociofacil/prod && sudo docker compose up -d && sudo docker compose ps
curl -sk -o /dev/null -w '%{http_code}\n' https://localhost/api/health
```
Expected: los tres servicios arriba y `200`.

- [ ] **Step 6: Agregar las suites `limits` e `isolation`**

Insertar antes de `main()` y sumarlas al `case` y a `all`.

```bash
suite_limits() {
  suite_header "Límites: prod por peso, dev por cap duro"

  # mem_limit se lee en bytes desde la API de Docker.
  local dev_app_mem prod_pg_mem prod_pg_oom dev_app_cpu
  dev_app_mem=$(docker inspect ngf-dev-app-1 \
    --format '{{.HostConfig.Memory}}' 2>/dev/null || echo 0)
  check_eq "dev app con 1536m de límite" "1610612736" "$dev_app_mem"

  prod_pg_mem=$(docker inspect ngf-prod-postgres-1 \
    --format '{{.HostConfig.Memory}}' 2>/dev/null || echo 0)
  check_eq "prod postgres con 1536m de límite" "1610612736" "$prod_pg_mem"

  prod_pg_oom=$(docker inspect ngf-prod-postgres-1 \
    --format '{{.HostConfig.OomScoreAdj}}' 2>/dev/null || echo 0)
  check_eq "prod postgres con oom_score_adj -500" "-500" "$prod_pg_oom"

  # NanoCpus: 0.75 core = 750000000
  dev_app_cpu=$(docker inspect ngf-dev-app-1 \
    --format '{{.HostConfig.NanoCpus}}' 2>/dev/null || echo 0)
  check_eq "dev app capada a 0.75 cores" "750000000" "$dev_app_cpu"

  # Prod NO debe tener cap: gana por peso, no por reserva.
  local prod_app_cpu
  prod_app_cpu=$(docker inspect ngf-prod-app-1 \
    --format '{{.HostConfig.NanoCpus}}' 2>/dev/null || echo -1)
  check_eq "prod app sin cap de CPU" "0" "$prod_app_cpu"
}

suite_isolation() {
  suite_header "Aislamiento entre stacks"

  check_cmd "volumen de prod existe" docker volume inspect ngf-prod_pgdata
  check_cmd "volumen de dev existe" docker volume inspect ngf-dev_pgdata

  # Redes separadas: dev no puede resolver el Postgres de prod.
  local dev_net prod_net
  dev_net=$(docker network ls --format '{{.Name}}' | grep -c '^ngf-dev_default$')
  prod_net=$(docker network ls --format '{{.Name}}' | grep -c '^ngf-prod_default$')
  check_eq "dev tiene su propia red" "1" "$dev_net"
  check_eq "prod tiene su propia red" "1" "$prod_net"

  if docker exec ngf-dev-app-1 getent hosts postgres 2>/dev/null \
     | grep -q .; then
    # Resuelve, pero debe ser el postgres de dev, no el de prod.
    local resolved dev_pg_ip
    resolved=$(docker exec ngf-dev-app-1 getent hosts postgres | awk '{print $1}')
    dev_pg_ip=$(docker inspect ngf-dev-postgres-1 \
      --format '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}')
    check_eq "dev resuelve su propio postgres" "$dev_pg_ip" "$resolved"
  else
    bad "dev no resuelve ningún postgres"
  fi
}
```

- [ ] **Step 7: Verificar el chequeo 3 de la spec — `down -v` de dev no toca prod**

Es el que previene borrar la base de un cliente creyendo que se limpia dev.

Este chequeo es **manual y de una sola vez**, a diferencia de los otros cinco: meter un `down -v` dentro de `verify-infra.sh` convertiría el script de verificación en algo que borra datos cada vez que se corre — exactamente el accidente que busca prevenir. Lo que sí queda en `suite_isolation` es la propiedad estructural que lo hace imposible: volúmenes con nombre prefijado por proyecto y redes separadas.

```bash
docker compose -p ngf-dev down -v
docker volume inspect ngf-prod_pgdata >/dev/null && echo "prod INTACTO"
docker volume inspect ngf-dev_pgdata 2>/dev/null || echo "dev borrado (correcto)"
docker compose -f docker/compose.dev.yml up -d
```
Expected: `prod INTACTO` y `dev borrado (correcto)`.

- [ ] **Step 8: Correr las suites**

Run: `./scripts/verify-infra.sh limits && ./scripts/verify-infra.sh isolation`
Expected: ambas en verde.

- [ ] **Step 9: Commit**

```bash
git add docker/compose.prod.yml docker/Caddyfile scripts/verify-infra.sh
git commit -m "feat: stack ngf-prod detras de Caddy con presupuesto de recursos"
```

---

## Task 10: Stack `ngf-stage` efímero

**Files:**
- Create: `docker/compose.stage.yml`

**Interfaces:**
- Consumes: imagen `ngf-app:<tag>` de la Task 7.
- Produce: stack `ngf-stage` levantable y destruible, con Postgres en tmpfs. Lo va a usar `deploy.sh` en el ciclo 3.

- [ ] **Step 1: Escribir `docker/compose.stage.yml`**

```yaml
# Stack de staging: corre la imagen ya buildeada contra un Postgres
# efímero. Empezar siempre de un estado conocido es justamente lo que
# valida una migración — por eso no comparte la base de dev.
name: ngf-stage

services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: ngf_stage
      POSTGRES_PASSWORD: efimero-no-persiste
      POSTGRES_DB: ngf_stage
      # PGDATA en un subdirectorio del tmpfs: el entrypoint lo crea y le
      # pone el owner correcto. Montar el tmpfs directamente en el
      # directorio de datos lo deja como root y initdb falla.
      PGDATA: /var/lib/postgresql/data/pgdata
    tmpfs:
      - /var/lib/postgresql/data:size=512m,mode=1777
    cpus: 0.25
    cpu_shares: 128
    mem_limit: 512m
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ngf_stage -d ngf_stage"]
      interval: 5s
      timeout: 3s
      retries: 10

  app:
    image: "ngf-app:${IMAGE_TAG:?IMAGE_TAG es obligatorio}"
    environment:
      NGF_STACK: stage
      DATABASE_URL: postgres://ngf_stage:efimero-no-persiste@postgres:5432/ngf_stage
    ports:
      - "100.64.81.63:3001:3000"
    cpus: 0.5
    cpu_shares: 128
    mem_limit: 768m
    depends_on:
      postgres:
        condition: service_healthy
```

- [ ] **Step 2: Frenar dev antes de levantar stage**

Sus caps sumados pasarían de un core, así que no corren juntos.

```bash
docker compose -p ngf-dev stop
```

- [ ] **Step 3: Levantar stage y verificar el healthcheck**

```bash
IMAGE_TAG=$(git rev-parse --short HEAD) \
  docker compose -f docker/compose.stage.yml up -d
sleep 15
curl -s http://100.64.81.63:3001/api/health | grep -q '"status":"ok"' \
  && echo "stage OK"
curl -s http://100.64.81.63:3001/ | grep -q 'stage' && echo "identifica el stack"
```
Expected: `stage OK` e `identifica el stack`.

- [ ] **Step 4: Verificar que es realmente efímero**

```bash
docker compose -p ngf-stage down -v
docker volume ls | grep ngf-stage || echo "sin volumenes residuales (correcto)"
docker compose -p ngf-dev start
```
Expected: `sin volumenes residuales (correcto)`.

- [ ] **Step 5: Commit**

```bash
git add docker/compose.stage.yml
git commit -m "feat: stack ngf-stage efimero con Postgres en tmpfs

El smoke test no pisa la base donde hay trabajo en curso."
```

---

## Task 11: Verificación bajo estrés y healthcheck con dientes

**Files:**
- Modify: `scripts/verify-infra.sh` (suites `stress` y `logs`)

**Interfaces:**
- Consumes: todas las suites anteriores.
- Produce: `./scripts/verify-infra.sh all` cubriendo los seis chequeos de la spec.

- [ ] **Step 1: Agregar las suites `stress` y `logs`**

Insertar antes de `main()` y sumarlas al `case` y a `all`.

```bash
suite_logs() {
  suite_header "Logs: rotación efectiva"

  # Chequeo 5 de la spec: que dev no pueda llenar el disco.
  #
  # OJO: no sirve mirar .HostConfig.LogConfig.Config del contenedor — la
  # rotación está puesta como default del daemon, y ahí sale vacío. Que el
  # default exista ya lo chequea suite_host; acá se mide el comportamiento
  # real, que es lo único que prueba que funciona.
  docker rm -f ngf-logspam >/dev/null 2>&1 || true
  docker run -d --name ngf-logspam alpine sh -c \
    'i=0; while [ $i -lt 60000 ]; do head -c 1000 /dev/zero | tr "\0" "x"; echo; i=$((i+1)); done' \
    >/dev/null 2>&1
  docker wait ngf-logspam >/dev/null 2>&1

  local logpath total
  logpath=$(docker inspect ngf-logspam --format '{{.LogPath}}' 2>/dev/null)
  if [[ -n "$logpath" && -f "$logpath" ]]; then
    # 3 archivos de 10m = 31 MB de techo, con margen de redondeo.
    total=$(du -cm "$logpath"* 2>/dev/null | tail -1 | awk '{print $1}')
    if [[ "${total:-9999}" -le 33 ]]; then
      ok "60 MB de logs quedaron en ${total} MB en disco (rotación activa)"
    else
      bad "60 MB de logs dejaron ${total} MB en disco (rotación NO activa)"
    fi
  else
    bad "no se pudo leer el LogPath del contenedor de prueba"
  fi
  docker rm -f ngf-logspam >/dev/null 2>&1 || true
}

suite_stress() {
  suite_header "Estrés: prod aguanta con dev saturada"

  # Chequeo 2 de la spec: el cgroup mata al contenedor, no el OOM del kernel.
  # `tail /dev/zero` aloca memoria sin techo — a diferencia de escribir en
  # /dev/shm, que se topa con su límite de 64m y falla por otra razón.
  local exit_code dmesg_before dmesg_after
  dmesg_before=$(dmesg 2>/dev/null | grep -c 'Out of memory' || true)
  docker run --rm --memory=64m --name ngf-memhog alpine \
    sh -c 'tail /dev/zero' >/dev/null 2>&1
  exit_code=$?
  check_eq "contenedor excedido muere por el cgroup (137)" "137" "$exit_code"
  dmesg_after=$(dmesg 2>/dev/null | grep -c 'Out of memory' || true)
  check_eq "el OOM del kernel no eligió víctima" "$dmesg_before" "$dmesg_after"

  # Chequeo 1 de la spec: p95 de /api/health bajo 500ms con dev saturada.
  docker run --rm -d --name ngf-cpuhog --cpus=1 --cpu-shares=256 \
    alpine sh -c 'while true; do :; done' >/dev/null 2>&1

  local times=() t p95 idx
  for _ in $(seq 1 20); do
    t=$(curl -sk -o /dev/null -w '%{time_total}' https://localhost/api/health)
    times+=("$(printf '%.0f' "$(echo "$t * 1000" | bc -l)")")
  done
  docker rm -f ngf-cpuhog >/dev/null 2>&1

  idx=$(printf '%s\n' "${times[@]}" | sort -n | tail -2 | head -1)
  p95=$idx
  if [[ "$p95" -lt 500 ]]; then
    ok "p95 de prod bajo carga: ${p95}ms (< 500ms)"
  else
    bad "p95 de prod bajo carga: ${p95}ms (>= 500ms)"
  fi

  # Chequeo 6 de la spec — el más importante: el healthcheck no miente.
  docker compose -p ngf-prod stop postgres >/dev/null 2>&1
  sleep 3
  local code
  code=$(curl -sk -o /tmp/ngf-degraded.json -w '%{http_code}' \
    https://localhost/api/health)
  check_eq "con Postgres caído el healthcheck da 503" "503" "$code"
  check_cmd "identifica que el check roto es postgres" \
    grep -q '"name":"postgres","ok":false' /tmp/ngf-degraded.json
  docker compose -p ngf-prod start postgres >/dev/null 2>&1
}
```

- [ ] **Step 2: Instalar `bc` si falta**

Run: `command -v bc || sudo apt-get install -y bc`

- [ ] **Step 3: Correr la verificación completa**

Run: `./scripts/verify-infra.sh all; echo "exit=$?"`
Expected: las siete suites en verde y `exit=0`. Si el chequeo 6 falla, **no seguir**: un healthcheck que devuelve 200 con Postgres caído deja al rollback automático sin criterio, y es la premisa de todo `deploy.sh`.

- [ ] **Step 4: Confirmar que prod se recuperó**

Run: `curl -sk https://localhost/api/health | grep -q '"status":"ok"' && echo "prod sano"`
Expected: `prod sano`. El paso anterior frenó Postgres a propósito.

- [ ] **Step 5: Commit**

```bash
git add scripts/verify-infra.sh
git commit -m "test: verificacion bajo estres y healthcheck con dientes

El chequeo 6 es el que importa: prueba que /api/health devuelve 503
con Postgres caido. Sin eso el rollback automatico no tiene criterio."
```

---

## Task 12: Actualizar CLAUDE.md y documentar el ciclo

**Files:**
- Modify: `CLAUDE.md`
- Create: `docs/runbook-stacks.md`

**Interfaces:**
- Consumes: todo lo anterior.
- Produce: documentación alineada con lo que se construyó.

- [ ] **Step 1: Corregir la sección de entorno en `CLAUDE.md`**

Dos desviaciones de la spec respecto del documento original. Reemplazar la frase que dice que el deploy "lo corre primero contra la base de dev" por:

```markdown
**Staging es la promoción del artefacto, no otra máquina.** El deploy buildea la imagen una sola vez, la corre en un tercer stack (`ngf-stage`) contra un Postgres **efímero en tmpfs** —no contra la base de dev, que suele tener trabajo en curso—, le pasa healthcheck y smoke tests, y recién entonces promueve **esa misma imagen** a producción. Nunca se rebuildea para prod: lo que se probó es exactamente lo que se sirve.

Dev y stage no corren a la vez: sus límites de CPU sumados pasarían de un core, así que el deploy frena `ngf-dev` antes de levantar `ngf-stage` y lo vuelve a arrancar al terminar.
```

- [ ] **Step 2: Agregar los tres bloqueantes a la sección de próximos pasos de `CLAUDE.md`**

```markdown
### Bloqueantes antes del primer tenant real

1. **Completar el healthcheck** con los checks de query filtrada por tenant y de pg-boss (`lib/health/checks.ts` tiene el pendiente anotado). Sin ellos el rollback automático se dispara con criterio incompleto.
2. **Backups** con `pg_dump` y restore verificado contra base descartable.
3. **`deploy.sh`** con su gate completo.
```

- [ ] **Step 3: Escribir `docs/runbook-stacks.md`**

```markdown
# Runbook: los tres stacks

| Stack | Levantar | Escucha en |
|---|---|---|
| dev | `docker compose -f docker/compose.dev.yml up -d` | `http://100.64.81.63:3000` |
| stage | `IMAGE_TAG=<sha> docker compose -f docker/compose.stage.yml up -d` | `http://100.64.81.63:3001` |
| prod | `cd /srv/negociofacil/prod && docker compose up -d` | `https://<ip-publica>` |

## Reglas

- **Nunca** `docker compose down -v` sin `-p` explícito.
- Dev y stage no corren juntos: frenar `ngf-dev` antes de levantar stage.
- Producción no se edita: se corre una imagen. Nada de editores en `/srv/negociofacil/prod/`.
- Buildear siempre con CPU limitada:
  `nice -n 15 docker build --cpuset-cpus=0 --memory=2g ...`

## Verificar que la infra sigue sana

    ./scripts/verify-infra.sh all

Correrlo después de cada deploy. Es lo que detecta que alguien aflojó un
límite — acordarse no es un mecanismo.
```

- [ ] **Step 4: Verificación final de todo el ciclo**

```bash
npm test
npm run build
./scripts/verify-infra.sh all
```
Expected: los tres en verde.

- [ ] **Step 5: Commit**

```bash
git add CLAUDE.md docs/runbook-stacks.md
git commit -m "docs: alinear CLAUDE.md con los tres stacks y agregar runbook"
```

---

## Al terminar

Con las 12 tareas cerradas quedan pendientes, en este orden:

1. **Ciclo 2 — backups:** `pg_dump` nocturno a object storage, restore verificado semanalmente contra base descartable.
2. **Ciclo 3 — `deploy.sh`:** el gate completo, que consume `ngf-stage` y los backups del ciclo 2.
3. **Completar el healthcheck** cuando existan `Tenant` y pg-boss.
