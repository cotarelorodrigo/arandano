# `deploy.sh` y su gate — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Un comando con gate que promueve código a producción, ensaya la migración contra una base descartable, verifica que lo que respondió sea lo que promovió, deja un tag de git y rollbackea solo si algo falla.

**Architecture:** `deploy.sh` orquesta y no decide: toda la lógica peligrosa vive en `scripts/lib/deploy-comun.sh` como funciones puras con unitarios que corren en milisegundos. El rollback automático **no reimplementa nada**: invoca `scripts/rollback.sh`, el mismo comando que correría una persona, así que los dos caminos comparten código por construcción y no por disciplina.

**Tech Stack:** bash, Docker Compose, Prisma CLI (imagen `arandano-migrate`), jq, git.

## Global Constraints

Copiados del spec (`docs/superpowers/specs/2026-08-06-deploy-design.md`) y de `CLAUDE.md`. Aplican a todas las tareas:

- **Builds**: siempre `--cgroup-parent=arandanobuild.slice --resource memory=2g --resource cpu-quota=100000`, con `--target` explícito y `--build-arg GIT_SHA`. `nice`, `--cpuset-cpus` y `--memory` son inertes en este host.
- **`arandano-dev` se frena antes del build**, no antes de stage. Aritmética: prod 3200 + dev 2304 + build 2048 + ~1,1 GB de sistema ≈ 8,5 GB sobre 7,6 GB.
- **Producción usa sólo `prisma migrate deploy`.** `migrate reset` y `db push` no aparecen en ningún script.
- **Nada de código fuente en `/srv/arandano/prod/`.** Ese directorio tiene compose, `.env`, Caddyfile y volúmenes.
- **Todos los scripts**: `set -euo pipefail`, `cd "$(dirname "${BASH_SOURCE[0]}")/.."` al inicio, y `log()`/`error()` con timestamp UTC como `backup.sh`.
- **Comentarios en castellano**, explicando *por qué*, siguiendo el estilo del repo.
- **Nunca imprimir contraseñas ni el contenido de un `.env`** en la salida.

## File Structure

| Archivo | Responsabilidad |
|---|---|
| `scripts/tests/correr-todos.sh` | Descubre y corre los `test-*.sh`. Sin lógica propia. |
| `scripts/lib/deploy-comun.sh` | Sólo funciones puras: deciden, no ejecutan. |
| `scripts/tests/test-deploy-comun.sh` | Unitarios de la lib. Sin Docker ni red. |
| `.githooks/pre-commit` | Un único chequeo: migración destructiva. |
| `scripts/smoke.sh` | Los smoke tests contra una URL base. Un caso por función. |
| `docker/compose.ensayo.yml` | Stack descartable con la forma de prod. |
| `scripts/rollback.sh` | Promover una imagen dada. Lo usan el operador y `deploy.sh`. |
| `scripts/deploy.sh` | La secuencia. Sin lógica de decisión adentro. |

---

### Task 1: Los tests de bash entran al gate

Hoy `scripts/tests/test-backup-comun.sh` no lo corre nadie: ni `npm test` ni `verify-infra.sh`. Como `npm test` es el paso 4 del gate que este plan construye, engancharlos ahí los pone en el camino crítico. Sin esto, los unitarios de las tareas siguientes nacen muertos.

Va primero a propósito: es la infraestructura de la que dependen todas las demás tareas.

**Files:**
- Create: `scripts/tests/correr-todos.sh`
- Modify: `package.json`

**Interfaces:**
- Consumes: nada.
- Produces: `npm test` corre todo `scripts/tests/test-*.sh` antes de vitest.

- [ ] **Step 1: Verificar que hoy no corre**

```bash
npm test 2>&1 | grep -c "test-backup-comun" || true
```
Expected: `0`. Confirma el problema antes de arreglarlo.

- [ ] **Step 2: Escribir el runner**

Crear `scripts/tests/correr-todos.sh`:

```bash
#!/usr/bin/env bash
# Corre todos los tests de bash del repo.
#
# Descubre por glob y no por lista: una lista es un lugar donde olvidarse de
# agregar el archivo nuevo, y un test que nadie corre es peor que no tenerlo,
# porque igual da la sensación de estar cubierto.
#
# Sin `set -e`: si un archivo falla, los demás tienen que correr igual. Ver
# todas las fallas de una vez es la diferencia entre un arreglo y cinco.
set -uo pipefail
cd "$(dirname "${BASH_SOURCE[0]}")/../.."

fallas=0
for archivo in scripts/tests/test-*.sh; do
  [[ -e "$archivo" ]] || { echo "no hay tests de bash que correr" >&2; exit 1; }
  printf '\n\033[1m%s\033[0m\n' "$archivo"
  bash "$archivo" || fallas=$((fallas + 1))
done

if [[ "$fallas" -ne 0 ]]; then
  printf '\n\033[31m%d archivo(s) de test de bash fallan\033[0m\n' "$fallas"
  exit 1
fi
```

```bash
chmod +x scripts/tests/correr-todos.sh
```

- [ ] **Step 3: Correrlo y verificar que pasa**

```bash
scripts/tests/correr-todos.sh
```
Expected: imprime `scripts/tests/test-backup-comun.sh` y termina en 0.

- [ ] **Step 4: Engancharlo a `npm test`**

En `package.json`, cambiar la línea del script `test`:

```json
    "test": "scripts/tests/correr-todos.sh && vitest run",
```

Los de bash van **primero**: corren en milisegundos y sin infraestructura, así que una falla de lógica pura se ve de inmediato en vez de después de que vitest levante un Postgres.

- [ ] **Step 5: Verificar que ahora sí corre**

```bash
npm test 2>&1 | grep -c "test-backup-comun"
```
Expected: mayor que 0.

```bash
npm test
```
Expected: los tests de bash y los 52 de vitest, todo en verde.

- [ ] **Step 6: Verificar que una falla de bash frena el gate**

```bash
printf '\nexit 1\n' >> scripts/tests/test-backup-comun.sh
npm test; echo "exit: $?"
git checkout scripts/tests/test-backup-comun.sh
```
Expected: exit distinto de 0, y vitest **no** llega a correr. Si vitest corre igual, el `&&` está mal puesto y el gate no protege.

- [ ] **Step 7: Commit**

```bash
git add scripts/tests/correr-todos.sh package.json
git commit -m "$(cat <<'EOF'
test: los tests de bash entran al gate

test-backup-comun.sh existía desde el ciclo de backups y no lo corría nadie:
ni npm test ni verify-infra.sh. Un test que nadie ejecuta es peor que no
tenerlo, porque igual da sensación de cobertura.

Van antes de vitest: corren en milisegundos y sin infraestructura, así que
una falla de lógica pura no espera a que levante un Postgres.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: La lib de decisiones

Todo lo que **decide** algo vive acá, y nada que ejecute. Es lo que permite que los tests corran sin Docker y sin red — la condición para que nadie tenga motivo de saltearlos.

**Files:**
- Create: `scripts/lib/deploy-comun.sh`
- Create: `scripts/tests/test-deploy-comun.sh`

**Interfaces:**
- Consumes: el runner de la Task 1.
- Produces:
  - `proxima_version <ultimo_tag> <minor|patch>` → `v1.MINOR.PATCH` por stdout; `v1.0.0` si el tag viene vacío; exit 1 si el formato no matchea.
  - `migracion_destructiva <sql>` → **exit 0 si ENCONTRÓ** algo destructivo (e imprime el patrón), exit 1 si está limpio. Convención de `grep`, no de booleano.
  - `mensaje_de_tag <sha> <migraciones>` → el texto del tag anotado.
  - `imagen_de_tag <mensaje>` → el sha; exit 1 si el mensaje no tiene la línea esperada.
  - `health_ok <json>` → exit 0 si sano.
  - `sha_del_health <json>` → el sha; exit 1 si falta.

- [ ] **Step 1: Escribir el test que falla**

Crear `scripts/tests/test-deploy-comun.sh`:

```bash
#!/usr/bin/env bash
# Tests unitarios de la lógica pura de deploy-comun.sh.
#
# Acá vive todo lo que decide si un deploy sigue o se frena, así que se prueba
# sin Docker, sin red y sin git: si estos tests necesitaran infraestructura,
# alguien terminaría salteándolos justo el día que importan.
set -uo pipefail
cd "$(dirname "$0")/../.."
source scripts/lib/deploy-comun.sh

PASS=0
FAIL=0
ok()  { printf '  \033[32m✓\033[0m %s\n' "$1"; PASS=$((PASS + 1)); }
bad() { printf '  \033[31m✗\033[0m %s\n' "$1"; FAIL=$((FAIL + 1)); }

check_eq() {
  local desc="$1" expected="$2" actual="$3"
  if [[ "$expected" == "$actual" ]]; then ok "$desc"
  else bad "$desc (esperado: $expected, obtenido: $actual)"; fi
}
check_true()  { if "${@:2}" >/dev/null 2>&1; then ok "$1"; else bad "$1 (esperaba éxito)"; fi; }
check_false() { if "${@:2}" >/dev/null 2>&1; then bad "$1 (esperaba fallo)"; else ok "$1"; fi; }

printf '\n\033[1mproxima_version\033[0m\n'
check_eq "sin tags arranca en v1.0.0"        "v1.0.0"  "$(proxima_version '' patch)"
check_eq "sin tags, minor también es v1.0.0" "v1.0.0"  "$(proxima_version '' minor)"
check_eq "patch sube el último número"       "v1.0.1"  "$(proxima_version v1.0.0 patch)"
# Sin aritmética de strings: v1.0.9 -> v1.0.10, no v1.0.91 ni v1.1.0.
check_eq "patch cruza la decena"             "v1.0.10" "$(proxima_version v1.0.9 patch)"
check_eq "minor sube y resetea el patch"     "v1.1.0"  "$(proxima_version v1.0.9 minor)"
check_eq "minor cruza la decena"             "v1.10.0" "$(proxima_version v1.9.4 minor)"
check_false "un tag con formato raro es error" proxima_version v2.0.0 patch
check_false "un tipo inventado es error"       proxima_version v1.0.0 cualquiera

printf '\n\033[1mmigracion_destructiva\033[0m\n'
# Exit 0 == encontró algo destructivo. Convención de grep.
check_true  "DROP COLUMN"    migracion_destructiva 'ALTER TABLE "tenants" DROP COLUMN "plan";'
check_true  "DROP TABLE"     migracion_destructiva 'DROP TABLE "clientes";'
check_true  "TRUNCATE"       migracion_destructiva 'TRUNCATE "ventas";'
check_true  "RENAME COLUMN"  migracion_destructiva 'ALTER TABLE "users" RENAME COLUMN "mail" TO "email";'
check_true  "cambio de tipo" migracion_destructiva 'ALTER TABLE "t" ALTER COLUMN "c" TYPE varchar(10);'
check_true  "en minúsculas"  migracion_destructiva 'alter table "t" drop column "c";'
# Lo aditivo tiene que pasar, o el gate se vuelve inservible y alguien lo apaga.
check_false "ADD COLUMN es aditivo"  migracion_destructiva 'ALTER TABLE "t" ADD COLUMN "c" TEXT;'
check_false "CREATE TABLE"           migracion_destructiva 'CREATE TABLE "t" ("id" TEXT);'
check_false "CREATE TYPE no es ALTER TYPE" migracion_destructiva 'CREATE TYPE "estado" AS ENUM ("TRIAL");'
check_false "CREATE INDEX"           migracion_destructiva 'CREATE INDEX "i" ON "t"("c");'
# Un DROP mencionado en un comentario no es un DROP.
check_false "comentario de línea"    migracion_destructiva '-- ojo: acá iba un DROP COLUMN
ALTER TABLE "t" ADD COLUMN "c" TEXT;'
# ...y uno escondido detrás de un comentario de bloque sí lo es.
check_true  "no se esconde tras un bloque" migracion_destructiva '/* nota */ DROP TABLE "t";'
# Regresión con el artefacto real: la migración inicial del núcleo es
# puramente aditiva y NO debe frenar ningún deploy.
check_false "la migración inicial real es aditiva" \
  migracion_destructiva "$(cat prisma/migrations/20260804205911_inicial/migration.sql)"

printf '\n\033[1mmensaje_de_tag / imagen_de_tag\033[0m\n'
MENSAJE="$(mensaje_de_tag 25297f7 20260804205911_inicial)"
check_true "el mensaje nombra la imagen" grep -q "^imagen: arandano-app:25297f7$" <<<"$MENSAJE"
check_true "el mensaje nombra las migraciones" grep -q "^migraciones: 20260804205911_inicial$" <<<"$MENSAJE"
check_eq "sin migraciones lo dice explícito" "migraciones: (ninguna)" \
  "$(mensaje_de_tag abc1234 '' | grep '^migraciones:')"
# La prueba que importa: son inversas. Si alguien cambia el formato de una y
# no de la otra, el rollback manual deja de encontrar a dónde volver.
check_eq "ida y vuelta" "25297f7" "$(imagen_de_tag "$MENSAJE")"
check_eq "ida y vuelta sin migraciones" "abc1234" \
  "$(imagen_de_tag "$(mensaje_de_tag abc1234 '')")"
check_false "un mensaje sin la línea de imagen es error" imagen_de_tag "una nota cualquiera"

printf '\n\033[1mhealth_ok / sha_del_health\033[0m\n'
SANO='{"status":"ok","checks":[{"name":"postgres","ok":true},{"name":"rol","ok":true}],"info":{"sha":"25297f7"}}'
ROTO='{"status":"error","checks":[{"name":"postgres","ok":true},{"name":"rol","ok":false}],"info":{"sha":"25297f7"}}'
MENTIROSO='{"status":"ok","checks":[{"name":"postgres","ok":true},{"name":"rol","ok":false}],"info":{"sha":"25297f7"}}'
VACIO='{"status":"ok","checks":[],"info":{"sha":"25297f7"}}'
check_true  "sano"                          health_ok "$SANO"
check_false "status error"                  health_ok "$ROTO"
# El caso que más importa: status ok con un check en false. Mirar sólo el
# status dejaría pasar un deploy con la app conectada como superusuario.
check_false "status ok con un check caído"  health_ok "$MENTIROSO"
# Cero checks no es salud, es ausencia de evidencia.
check_false "sin ningún check no está sano" health_ok "$VACIO"
check_false "un JSON inválido no está sano" health_ok 'esto no es json'
check_eq "extrae el sha" "25297f7" "$(sha_del_health "$SANO")"
check_false "sin info.sha es error" sha_del_health '{"status":"ok","checks":[{"ok":true}]}'

printf '\n%d ok, %d fallan\n' "$PASS" "$FAIL"
[[ "$FAIL" -eq 0 ]]
```

```bash
chmod +x scripts/tests/test-deploy-comun.sh
```

- [ ] **Step 2: Correr el test y verificar que falla**

```bash
scripts/tests/test-deploy-comun.sh; echo "exit: $?"
```
Expected: falla con `scripts/lib/deploy-comun.sh: No such file or directory`.

- [ ] **Step 3: Escribir la lib**

Crear `scripts/lib/deploy-comun.sh`:

```bash
#!/usr/bin/env bash
# Lógica de decisión compartida entre deploy.sh y rollback.sh.
#
# Sólo funciones PURAS: reciben strings, devuelven strings o un código de
# salida, y no tocan Docker, la red, git ni el disco. Eso es lo que permite que
# scripts/tests/test-deploy-comun.sh corra en milisegundos, y por lo tanto que
# nadie tenga excusa para saltearlo.
#
# mensaje_de_tag e imagen_de_tag son INVERSAS y viven juntas a propósito:
# deploy.sh escribe el mensaje del tag y rollback.sh lo lee. Separarlas en dos
# archivos es exactamente cómo se desincronizan, y el modo de falla sería que
# el rollback manual no encuentre a dónde volver — descubierto justo el día que
# hace falta.
#
# Este archivo sólo define funciones. No ejecuta nada al sourcearse.

log()   { printf '%s  %s\n' "$(date -u +%H:%M:%S)" "$*"; }
error() { printf '%s  ERROR: %s\n' "$(date -u +%H:%M:%S)" "$*" >&2; }

# La próxima versión, derivada del último tag. Con el tag vacío devuelve
# v1.0.0: ese es el primer deploy, y el caso vive acá y no en el llamador
# justamente para que tenga test.
#
# MAJOR se queda en 1 por decisión de producto (CLAUDE.md): esto es un SaaS sin
# API pública. Un tag v2.x.y es señal de que algo lo escribió a mano, así que se
# rechaza en vez de interpretarse.
proxima_version() {
  local ultimo="${1:-}" tipo="${2:-}"

  case "$tipo" in
    minor|patch) ;;
    *) error "tipo de versión inválido: '${tipo}' (válidos: minor, patch)"; return 1 ;;
  esac

  if [[ -z "$ultimo" ]]; then
    printf 'v1.0.0\n'
    return 0
  fi

  if [[ ! "$ultimo" =~ ^v1\.([0-9]+)\.([0-9]+)$ ]]; then
    error "tag con formato inesperado: '${ultimo}' (se espera v1.MINOR.PATCH)"
    return 1
  fi

  local minor="${BASH_REMATCH[1]}" patch="${BASH_REMATCH[2]}"
  # $((...)) y no concatenación de strings: v1.0.9 + patch es v1.0.10.
  if [[ "$tipo" == minor ]]; then
    printf 'v1.%d.0\n' "$((minor + 1))"
  else
    printf 'v1.%d.%d\n' "$minor" "$((patch + 1))"
  fi
}

# ¿El SQL trae algo que rompa el rollback? Exit 0 si SÍ encontró (y lo imprime),
# exit 1 si está limpio. Es la convención de grep, no la de un booleano: en bash
# "encontré" y "todo bien" no pueden ser el mismo 0.
#
# El criterio no es "destruye datos" sino "el código ANTERIOR deja de funcionar
# contra este schema". Por eso entran los renames, que no borran nada: el
# rollback revierte la imagen y no la base, así que una columna renombrada deja
# a la imagen vieja consultando algo que ya no existe.
migracion_destructiva() {
  local sql="${1:-}"

  # Los comentarios se sacan ANTES de buscar. Un "-- acá iba un DROP COLUMN" en
  # una nota no es una migración destructiva; frenar el deploy por eso enseña a
  # ignorar el chequeo. Los de bloque se sacan primero porque pueden contener
  # un `--` adentro.
  local limpio
  limpio=$(printf '%s' "$sql" \
    | perl -0777 -pe 's{/\*.*?\*/}{ }gs' \
    | sed -e 's|--.*$||' \
    | tr '\n' ' ')

  local patron
  for patron in \
    'DROP[[:space:]]+COLUMN' \
    'DROP[[:space:]]+TABLE' \
    'DROP[[:space:]]+SCHEMA' \
    'DROP[[:space:]]+CONSTRAINT' \
    'TRUNCATE' \
    'RENAME[[:space:]]+TO' \
    'RENAME[[:space:]]+COLUMN' \
    'ALTER[[:space:]]+COLUMN[[:space:]]+[^;]*[[:space:]]TYPE[[:space:]]'
  do
    if printf '%s' "$limpio" | grep -qiE "$patron"; then
      printf '%s\n' "$patron"
      return 0
    fi
  done

  return 1
}

# El cuerpo del tag anotado. Lleva lo que el SHA no dice: qué imagen se promovió
# y qué migraciones corrieron en ese deploy. Es lo primero que alguien quiere
# leer a las 11 de la noche.
mensaje_de_tag() {
  local sha="${1:-}" migraciones="${2:-}"
  [[ -n "$sha" ]] || { error "mensaje_de_tag necesita un sha"; return 1; }
  [[ -n "$migraciones" ]] || migraciones="(ninguna)"
  printf 'imagen: arandano-app:%s\nmigraciones: %s\n' "$sha" "$migraciones"
}

# La inversa de mensaje_de_tag: de dónde saca rollback.sh a qué imagen volver.
imagen_de_tag() {
  local mensaje="${1:-}" sha
  sha=$(printf '%s\n' "$mensaje" \
        | sed -nE 's|^imagen: arandano-app:([0-9a-f]+)[[:space:]]*$|\1|p' \
        | head -1)
  if [[ -z "$sha" ]]; then
    error "el mensaje del tag no tiene una línea 'imagen: arandano-app:<sha>'"
    return 1
  fi
  printf '%s\n' "$sha"
}

# Sano es: status ok, AL MENOS un check, y NINGÚN check en false.
#
# Los tres a la vez, y no sólo el status: un `status: ok` con un check caído
# dejaría pasar un deploy con la app conectada como superusuario, que es
# exactamente lo que el check `rol` existe para atrapar. Y cero checks no es
# salud, es ausencia de evidencia.
health_ok() {
  local json="${1:-}"
  printf '%s' "$json" | jq -e '
    .status == "ok"
    and (.checks | length) > 0
    and ([.checks[] | select(.ok != true)] | length) == 0
  ' >/dev/null 2>&1
}

# El SHA que la app dice estar corriendo. deploy.sh lo compara contra el tag que
# promovió: sin eso, un healthcheck en 200 desde el contenedor VIEJO se lee como
# deploy exitoso.
sha_del_health() {
  local json="${1:-}" sha
  sha=$(printf '%s' "$json" | jq -re '.info.sha // empty' 2>/dev/null) || true
  if [[ -z "$sha" ]]; then
    error "el healthcheck no reportó info.sha"
    return 1
  fi
  printf '%s\n' "$sha"
}
```

- [ ] **Step 4: Correr el test y verificar que pasa**

```bash
scripts/tests/test-deploy-comun.sh
```
Expected: todos en verde, `0 fallan`.

Si falla `la migración inicial real es aditiva`, **no relajar el patrón sin mirar**: leer qué encontró. La migración inicial tiene `CREATE TYPE`, que no debe matchear `ALTER COLUMN ... TYPE`.

- [ ] **Step 5: Verificar que `perl` está en el host**

```bash
command -v perl
```
Expected: una ruta. Si no está, reemplazar la línea de `perl` por `sed -e 's|/\*[^*]*\*/| |g'` y ajustar el test del comentario de bloque — pero verificarlo, no asumirlo.

- [ ] **Step 6: Correr la suite completa**

```bash
npm test
```
Expected: los dos archivos de bash y los 52 de vitest, todo en verde.

- [ ] **Step 7: Commit**

```bash
git add scripts/lib/deploy-comun.sh scripts/tests/test-deploy-comun.sh
git commit -m "$(cat <<'EOF'
feat(deploy): la lógica de decisión del gate, con sus unitarios

Funciones puras: proxima_version, migracion_destructiva, el par
mensaje_de_tag/imagen_de_tag, y el parseo del healthcheck. Nada acá toca
Docker, la red, git ni el disco, así que los tests corren en milisegundos y
nadie tiene motivo para saltearlos.

migracion_destructiva mira "el código anterior deja de andar", no "borra
datos": por eso entran los renames, que no borran nada pero rompen el
rollback igual, porque el rollback revierte la imagen y no la base.

health_ok exige status ok, al menos un check y ninguno en false. Mirar sólo
el status dejaría pasar un deploy con la app conectada como superusuario.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: El hook de pre-commit

El único de los tres chequeos que no necesita levantar nada: lee SQL y busca patrones. Por eso es el único que tiene sentido como hook. Los otros dos —schema contra migraciones, y migraciones contra producción— necesitan una base, y un hook que levanta un Postgres en cada commit sobre 2 vCPU termina con alguien usando `--no-verify`.

**Files:**
- Create: `.githooks/pre-commit`
- Modify: `scripts/setup-host.sh`

**Interfaces:**
- Consumes: `migracion_destructiva` de la Task 2.
- Produces: nada que otra tarea use. `deploy.sh` repite el chequeo por su cuenta (Task 7), porque un hook se puede saltear.

- [ ] **Step 1: Escribir el hook**

Crear `.githooks/pre-commit`:

```bash
#!/usr/bin/env bash
# Frena un commit que traiga una migración destructiva.
#
# Vive versionado en .githooks/ y no en .git/hooks/, que no se versiona. Se
# activa con `git config core.hooksPath .githooks` — línea que está en
# scripts/setup-host.sh, porque un hook que hay que acordarse de instalar no
# está instalado.
#
# Este es el ÚNICO chequeo que va acá. Los otros dos del gate necesitan
# levantar un Postgres, y un hook que hace eso en cada commit sobre 2 vCPU
# termina con alguien usando --no-verify. Un hook desactivado no protege nada.
#
# Es una red temprana, no la definitiva: deploy.sh vuelve a chequear lo mismo,
# porque --no-verify existe.
set -uo pipefail
cd "$(git rev-parse --show-toplevel)"
source scripts/lib/deploy-comun.sh

archivos=$(git diff --cached --name-only --diff-filter=ACM -- 'prisma/migrations/**/migration.sql')
[[ -n "$archivos" ]] || exit 0

encontrado=0
while IFS= read -r archivo; do
  # El contenido STAGEADO, no el del working tree: es lo que se va a commitear.
  sql=$(git show ":$archivo")
  if patron=$(migracion_destructiva "$sql"); then
    error "migración destructiva en $archivo (patrón: $patron)"
    encontrado=1
  fi
done <<< "$archivos"

if [[ "$encontrado" -ne 0 ]]; then
  cat >&2 <<'EOF'

El rollback automático revierte la IMAGEN, no la base. Si el schema nuevo no
soporta la versión anterior del código, el rollback no funciona y —sin feature
flags— no queda ninguna red.

Expand/contract: primero se deploya el código que deja de usar la columna, y
el DROP viaja en un deploy POSTERIOR.

Si de verdad corresponde (una limpieza de contract cuyo código ya no está en
producción hace varios deploys), commitear con --no-verify y dejar dicho en el
mensaje por qué es seguro.
EOF
  exit 1
fi
```

```bash
chmod +x .githooks/pre-commit
```

- [ ] **Step 2: Probar que deja pasar lo aditivo**

```bash
mkdir -p prisma/migrations/29990101000000_prueba_aditiva
echo 'ALTER TABLE "tenants" ADD COLUMN "nota" TEXT;' \
  > prisma/migrations/29990101000000_prueba_aditiva/migration.sql
git add prisma/migrations/29990101000000_prueba_aditiva/migration.sql
.githooks/pre-commit; echo "exit: $?"
```
Expected: `exit: 0`, sin salida.

- [ ] **Step 3: Probar que frena lo destructivo**

```bash
echo 'ALTER TABLE "tenants" DROP COLUMN "nota";' \
  > prisma/migrations/29990101000000_prueba_aditiva/migration.sql
git add prisma/migrations/29990101000000_prueba_aditiva/migration.sql
.githooks/pre-commit; echo "exit: $?"
```
Expected: `exit: 1`, con el mensaje de expand/contract.

- [ ] **Step 4: Limpiar la migración de prueba**

```bash
git restore --staged prisma/migrations/29990101000000_prueba_aditiva/migration.sql
rm -rf prisma/migrations/29990101000000_prueba_aditiva
git status --short
```
Expected: sin rastros de la migración de prueba.

- [ ] **Step 5: Activarlo y dejarlo en la receta del host**

```bash
git config core.hooksPath .githooks
git config --get core.hooksPath
```
Expected: `.githooks`

En `scripts/setup-host.sh`, agregar al final de la sección que prepara el repo (o crear una si no existe), con su comentario:

```bash
# Los hooks viven versionados en .githooks/, no en .git/hooks/ (que no se
# versiona y no viaja con el clon). Sin esta línea, el chequeo de migraciones
# destructivas existe en el repo y no corre en la máquina.
git -C /root/arandano config core.hooksPath .githooks
```

- [ ] **Step 6: Verificar que el hook corre de verdad en un commit**

```bash
mkdir -p prisma/migrations/29990101000000_prueba_hook
echo 'DROP TABLE "clientes";' > prisma/migrations/29990101000000_prueba_hook/migration.sql
git add prisma/migrations/29990101000000_prueba_hook/migration.sql
git commit -m "prueba que no debe entrar"; echo "exit: $?"
git restore --staged prisma/migrations/29990101000000_prueba_hook/migration.sql
rm -rf prisma/migrations/29990101000000_prueba_hook
```
Expected: el commit **falla** con el mensaje del hook. Esto prueba el cableado, no la función.

- [ ] **Step 7: Commit**

```bash
git add .githooks/pre-commit scripts/setup-host.sh
git commit -m "$(cat <<'EOF'
feat(deploy): hook que frena una migración destructiva antes del commit

Es el único de los tres chequeos del gate que no necesita levantar una base,
y por eso el único que tiene sentido como hook: uno que arranque un Postgres
en cada commit sobre 2 vCPU termina con alguien usando --no-verify.

Red temprana, no definitiva. deploy.sh repite el chequeo, justamente porque
--no-verify existe.

Vive en .githooks/ versionado, y setup-host.sh fija core.hooksPath: un hook
que hay que acordarse de instalar no está instalado.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: `scripts/smoke.sh`

Los smoke tests contra una URL base. Se escribe con un caso por función y una lista, de modo que sumar uno sea agregar una función — porque la lista real (login, ventas, facturación, órdenes de trabajo, catálogo) llega cuando exista ese código.

**Files:**
- Create: `scripts/smoke.sh`

**Interfaces:**
- Consumes: `health_ok` y `sha_del_health` de la Task 2.
- Produces: `scripts/smoke.sh <url_base> <sha_esperado>` → exit 0 si todo pasa. Lo llama `deploy.sh` en el paso 9.

- [ ] **Step 1: Escribir el script**

Crear `scripts/smoke.sh`:

```bash
#!/usr/bin/env bash
# Smoke tests: los caminos que más duelen, contra un stack ya levantado.
#
# Recibe la URL base por argumento en vez de fijarla, porque el mismo archivo
# corre contra arandano-stage durante el gate y podría correr contra cualquier
# otro stack. Nunca contra producción con datos de clientes: acá se escribe.
#
# Un caso por función, y la lista abajo. Sumar un caso es agregar una función y
# un renglón — eso importa porque hoy sólo existen /api/health y /, y la lista
# de verdad (login, venta, factura, orden de trabajo, catálogo) llega cuando
# exista ese código.
set -uo pipefail
cd "$(dirname "${BASH_SOURCE[0]}")/.."
source scripts/lib/deploy-comun.sh

URL_BASE="${1:-}"
SHA_ESPERADO="${2:-}"
if [[ -z "$URL_BASE" || -z "$SHA_ESPERADO" ]]; then
  echo "uso: smoke.sh <url_base> <sha_esperado>" >&2
  exit 2
fi

PASS=0
FAIL=0
ok()  { printf '  \033[32m✓\033[0m %s\n' "$1"; PASS=$((PASS + 1)); }
bad() { printf '  \033[31m✗\033[0m %s\n' "$1"; FAIL=$((FAIL + 1)); }

# Una sola llamada al healthcheck, reusada por varios casos: cada request es un
# ida y vuelta a Postgres con un pool de máximo 5, y golpearlo cuatro veces
# para preguntar cuatro cosas del mismo JSON no prueba nada extra.
SALUD=$(curl -fsS --max-time 10 "$URL_BASE/api/health" 2>/dev/null) || SALUD=""

caso_health_responde() {
  [[ -n "$SALUD" ]]
}

caso_health_sano() {
  health_ok "$SALUD"
}

# Que la app esté hablando con la base que cree, y no con la de otro stack.
caso_sha_esperado() {
  local sha
  sha=$(sha_del_health "$SALUD") || return 1
  [[ "$sha" == "$SHA_ESPERADO" ]]
}

# El check `rol` reporta con qué rol está conectada. Si dice superusuario, las
# policies de RLS no aplican y el aislamiento entre tenants es decorativo.
caso_rol_sin_privilegios() {
  printf '%s' "$SALUD" | jq -e '
    [.checks[] | select(.name == "rol")] | length == 1
    and (.[0].ok == true)
    and (.[0].detail | test("rol=arandano_app"))
  ' >/dev/null 2>&1
}

caso_home_responde() {
  curl -fsS --max-time 10 -o /dev/null "$URL_BASE/"
}

printf '\n\033[1mSmoke: %s\033[0m\n' "$URL_BASE"
for caso in \
  caso_health_responde \
  caso_health_sano \
  caso_sha_esperado \
  caso_rol_sin_privilegios \
  caso_home_responde
do
  if "$caso"; then ok "$caso"; else bad "$caso"; fi
done

printf '\n%d ok, %d fallan\n' "$PASS" "$FAIL"
[[ "$FAIL" -eq 0 ]]
```

```bash
chmod +x scripts/smoke.sh
```

- [ ] **Step 2: Correrlo contra producción, que está viva y sana**

Producción hoy corre `25297f7` y está sana, así que sirve como banco de pruebas del script. **Es de sólo lectura**: dos GET, nada que escriba.

```bash
scripts/smoke.sh http://127.0.0.1 25297f7
```
Expected: `5 ok, 0 fallan`.

- [ ] **Step 3: Verificar que detecta un sha equivocado**

```bash
scripts/smoke.sh http://127.0.0.1 0000000; echo "exit: $?"
```
Expected: falla `caso_sha_esperado` y sólo ese; exit distinto de 0. Si pasa igual, la comparación de sha no sirve y el bloqueante 8 queda sin cubrir.

- [ ] **Step 4: Verificar que detecta un stack caído**

```bash
scripts/smoke.sh http://127.0.0.1:9999 25297f7; echo "exit: $?"
```
Expected: fallan todos los casos que dependen del healthcheck; exit distinto de 0. Sin cuelgue: el `--max-time` corta.

- [ ] **Step 5: Commit**

```bash
git add scripts/smoke.sh
git commit -m "$(cat <<'EOF'
feat(deploy): smoke tests contra un stack levantado

Recibe la URL base por argumento porque corre contra stage durante el gate, y
un caso por función para que sumar uno sea agregar una función.

Los casos de hoy son los que hoy se pueden probar: que el healthcheck
responda y esté sano de verdad (todos sus checks, no sólo el status), que el
sha reportado sea el que se promovió, que el rol de conexión no tenga
privilegios, y que la home conteste. Login, ventas, facturación, órdenes de
trabajo y catálogo entran cuando exista ese código.

Una sola llamada al healthcheck reusada por varios casos: cada request es un
ida y vuelta a Postgres con pool de 5.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: El stack de ensayo

Un cuarto stack con la **forma** de producción pero desechable, para poder correr la secuencia completa de `deploy.sh` sin tocar clientes. La forma importa: si el ensayo no promoviera igual que prod —`.env` con `IMAGE_TAG`, `docker compose up -d --force-recreate app`— no probaría el paso que más riesgo tiene.

**Files:**
- Create: `docker/compose.ensayo.yml`
- Create: `/srv/arandano/ensayo/` (fuera del repo)

**Interfaces:**
- Consumes: nada.
- Produces: un stack `arandano-ensayo` con `/srv/arandano/ensayo/{docker-compose.yml,.env}`, healthcheck en `http://100.64.81.63:3002/api/health`.

- [ ] **Step 1: Escribir el compose**

Crear `docker/compose.ensayo.yml`:

```yaml
# Stack de ensayo: la forma de producción, pero desechable.
#
# Existe para una sola cosa: correr la secuencia COMPLETA de deploy.sh sin
# tocar clientes. Por eso copia la estructura de compose.prod.yml —env_file con
# IMAGE_TAG interpolado, healthcheck, force-recreate— y no la de stage: si el
# ensayo promoviera distinto que prod, el paso de promoción quedaría sin probar,
# que es justo el que dispara el rollback.
#
# Lo que NO copia son los límites de recursos. Un ensayo corre CON producción
# arriba, y en el tramo del ensayo en stage pueden coexistir los dos stacks
# descartables: prod 3200 + stage 1280 + ensayo 1280 + ~1,1 GB de sistema
# ≈ 6,9 GB sobre 7,6. Entra sólo porque deploy.sh frenó arandano-dev y sus
# 2304 MiB. Con los límites de prod (3200) no entraría.
name: arandano-ensayo

services:
  postgres:
    image: postgres:17-alpine
    env_file: .env
    # Base efímera en tmpfs: cada ensayo arranca de cero, que es lo que hace
    # que el ensayo de la migración signifique algo. Mismos valores medidos que
    # stage y verify-backup.sh: el tmpfs va por debajo de mem_limit porque sus
    # páginas cuentan 1:1 contra el cgroup y no son page cache descartable.
    tmpfs:
      - /var/lib/postgresql/data:size=320m,mode=1777
    cpus: 0.25
    cpu_shares: 128
    mem_limit: 512m
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      timeout: 3s
      retries: 10

  app:
    image: "arandano-app:${IMAGE_TAG:?IMAGE_TAG es obligatorio}"
    env_file: .env
    environment:
      ARANDANO_STACK: ensayo
      ARANDANO_DB_ESPERADA: arandano_ensayo
    # Sólo por Tailscale, como dev y stage. Este stack no existe desde internet.
    ports:
      - "100.64.81.63:3002:3000"
    cpus: 0.5
    cpu_shares: 128
    mem_limit: 768m
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:3000/api/health"]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 30s
    depends_on:
      postgres:
        condition: service_healthy
```

- [ ] **Step 2: Crear el directorio de runtime, con la forma de prod**

```bash
mkdir -p /srv/arandano/ensayo
cp docker/compose.ensayo.yml /srv/arandano/ensayo/docker-compose.yml

PG_PASS=$(openssl rand -hex 24)
umask 077
cat > /srv/arandano/ensayo/.env <<EOF
POSTGRES_USER=arandano_ensayo_super
POSTGRES_PASSWORD=${PG_PASS}
POSTGRES_DB=arandano_ensayo
IMAGE_TAG=25297f7
EOF
chmod 600 /srv/arandano/ensayo/.env
ls -la /srv/arandano/ensayo/
```
Expected: `docker-compose.yml` y un `.env` en `600`.

`openssl rand -hex` y no `-base64`: el alfabeto base64 incluye `/` y `+`, que dentro de una URL de conexión rompen el parseo o se malinterpretan en silencio.

Las dos URLs (`DATABASE_URL` y `MIGRATE_DATABASE_URL`) **no** van todavía: las escribe `deploy.sh` después de crear los roles, igual que en producción.

- [ ] **Step 3: Probar que levanta y que la app queda esperando roles**

```bash
cd /srv/arandano/ensayo && docker compose up -d postgres && sleep 5 && docker compose ps
```
Expected: `arandano-ensayo-postgres-1` sano. La app todavía no se levanta: sin roles ni `DATABASE_URL`, no podría conectarse — y es lo esperado, igual que en stage.

- [ ] **Step 4: Verificar el aislamiento**

```bash
docker volume ls | grep -c arandano-ensayo
docker network ls | grep arandano-ensayo
```
Expected: 0 volúmenes (la base es tmpfs, no tiene volumen) y su propia red `arandano-ensayo_default`.

- [ ] **Step 5: Bajarlo**

```bash
cd /srv/arandano/ensayo && docker compose down -v
cd /root/arandano
docker ps --filter name=arandano-ensayo --format '{{.Names}}'
```
Expected: sin salida.

- [ ] **Step 6: Commit**

```bash
git add docker/compose.ensayo.yml
git commit -m "$(cat <<'EOF'
feat(deploy): stack de ensayo con la forma de producción

Existe para correr la secuencia completa de deploy.sh sin tocar clientes.
Copia la estructura de prod —env_file con IMAGE_TAG, healthcheck,
force-recreate— porque si promoviera distinto, el paso de promoción quedaría
sin probar, y es el que dispara el rollback.

No copia los límites de prod: un ensayo corre CON producción arriba, y en el
tramo del ensayo en stage coexisten los dos descartables. Con los límites de
prod la caja no alcanzaría.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: `scripts/rollback.sh`

Va **antes** que `deploy.sh` a propósito: el rollback automático de `deploy.sh` no reimplementa nada, invoca este script. Así los dos caminos comparten código por construcción, y el automático ejercita exactamente el comando que correría una persona.

**Files:**
- Create: `scripts/rollback.sh`

**Interfaces:**
- Consumes: `imagen_de_tag`, `health_ok`, `sha_del_health` de la Task 2.
- Produces: `scripts/rollback.sh [--a=<sha>] [--objetivo=prod|ensayo]`. Sin `--a`, deriva del anteúltimo tag. Exit 0 si el destino quedó sano.

- [ ] **Step 1: Escribir el script**

Crear `scripts/rollback.sh`:

```bash
#!/usr/bin/env bash
# Volver a una imagen anterior. Un comando, una línea.
#
# Lo usan dos: el operador cuando el healthcheck no vio algo, y deploy.sh como
# su rollback automático. Es el MISMO código a propósito — si el automático
# tuviera su propia copia, el camino que se ejercita todos los días sería el
# que nunca se usa en una emergencia.
#
# NO TOCA LA BASE DE DATOS. Nunca. Revierte la imagen y nada más. Por eso el
# gate de deploy.sh se niega ante una migración destructiva: si el schema nuevo
# no soporta el código viejo, esto no alcanza y no queda ninguna red.
set -euo pipefail
cd "$(dirname "${BASH_SOURCE[0]}")/.."
source scripts/lib/deploy-comun.sh

DESTINO=""
OBJETIVO=prod

uso() {
  cat >&2 <<'EOF'
uso: rollback.sh [--a=<sha>] [--objetivo=prod|ensayo]

  --a         sha de la imagen a la que volver. Sin esto, se deriva del
              ANTEÚLTIMO tag de git: el último describe lo que está corriendo.
  --objetivo  qué stack. prod (default) o ensayo.
EOF
  exit 2
}

for arg in "$@"; do
  case "$arg" in
    --a=*)        DESTINO="${arg#*=}" ;;
    --objetivo=*) OBJETIVO="${arg#*=}" ;;
    -h|--help)    uso ;;
    *) error "argumento desconocido: $arg"; uso ;;
  esac
done

case "$OBJETIVO" in
  prod)   DIR=/srv/arandano/prod;   URL_SALUD=http://127.0.0.1 ;;
  ensayo) DIR=/srv/arandano/ensayo; URL_SALUD=http://100.64.81.63:3002 ;;
  *) error "objetivo inválido: $OBJETIVO"; uso ;;
esac

# Sin --a: el ANTEÚLTIMO tag. El último describe lo que está corriendo ahora,
# así que volver ahí no sería volver a ningún lado.
if [[ -z "$DESTINO" ]]; then
  anteultimo=$(git tag --list 'v1.*' --sort=-v:refname | sed -n '2p')
  if [[ -z "$anteultimo" ]]; then
    error "no hay un tag anterior al que volver (hacen falta al menos dos)"
    error "si sabés a qué imagen volver, pasala: rollback.sh --a=<sha>"
    exit 1
  fi
  DESTINO=$(imagen_de_tag "$(git tag -l --format='%(contents)' "$anteultimo")")
  log "destino derivado de $anteultimo: $DESTINO"
fi

# Que la imagen exista ANTES de tocar el .env. Escribir un IMAGE_TAG que apunta
# a una imagen inexistente deja el stack sin poder levantar, y convierte un
# rollback en una caída más larga.
if ! docker image inspect "arandano-app:$DESTINO" >/dev/null 2>&1; then
  error "la imagen arandano-app:$DESTINO no existe en este host"
  error "imágenes disponibles:"
  docker images arandano-app --format '  {{.Tag}}  ({{.CreatedAt}})' >&2
  exit 1
fi

ACTUAL=$(grep -oP '^IMAGE_TAG=\K.*' "$DIR/.env" || echo "(desconocido)")
log "rollback en $OBJETIVO: $ACTUAL -> $DESTINO"
log "la base de datos NO se toca"

sed -i "s|^IMAGE_TAG=.*|IMAGE_TAG=${DESTINO}|" "$DIR/.env"
( cd "$DIR" && docker compose up -d --force-recreate app )

# Mismo plazo que el healthcheck del deploy: 90s. Un rollback que espera para
# siempre no avisa que falló.
log "esperando a que $OBJETIVO responda sano"
limite=$((SECONDS + 90))
salud=""
while (( SECONDS < limite )); do
  salud=$(curl -fsS --max-time 5 "$URL_SALUD/api/health" 2>/dev/null) || salud=""
  if [[ -n "$salud" ]] && health_ok "$salud" \
     && [[ "$(sha_del_health "$salud")" == "$DESTINO" ]]; then
    log "rollback ok: $OBJETIVO responde sano con sha=$DESTINO"
    exit 0
  fi
  sleep 3
done

# El peor caso. No reintenta ni entra en loop: lo único útil acá es que una
# persona tenga los datos sin salir a buscarlos.
error "EL ROLLBACK NO VERIFICÓ EN 90s"
error "  stack:        $OBJETIVO ($DIR)"
error "  venía de:     $ACTUAL"
error "  se intentó:   $DESTINO"
error "  último JSON:  ${salud:-<sin respuesta>}"
error ""
error "para ver qué pasa:  docker compose -f $DIR/docker-compose.yml logs --tail=50 app"
error "para probar otra:   scripts/rollback.sh --a=<sha> --objetivo=$OBJETIVO"
exit 1
```

```bash
chmod +x scripts/rollback.sh
```

- [ ] **Step 2: Verificar que se niega ante una imagen inexistente**

```bash
scripts/rollback.sh --a=0000000 --objetivo=ensayo; echo "exit: $?"
```
Expected: exit 1, con el mensaje de que la imagen no existe y la lista de las que sí. **El `.env` de ensayo no debe haber cambiado** — verificarlo:

```bash
grep '^IMAGE_TAG=' /srv/arandano/ensayo/.env
```
Expected: sigue en `25297f7`.

- [ ] **Step 3: Verificar que se niega sin tags**

```bash
git tag --list 'v1.*' | wc -l
scripts/rollback.sh --objetivo=ensayo; echo "exit: $?"
```
Expected: hoy no hay tags, así que exit 1 con el mensaje de que hacen falta al menos dos, y la sugerencia de `--a=`.

- [ ] **Step 4: Verificar el rechazo de un objetivo inválido**

```bash
scripts/rollback.sh --a=25297f7 --objetivo=produccion; echo "exit: $?"
```
Expected: exit 2 con el uso. No debe tocar nada.

- [ ] **Step 5: Commit**

```bash
git add scripts/rollback.sh
git commit -m "$(cat <<'EOF'
feat(deploy): rollback.sh, el comando de una línea

Sin --a deriva del ANTEÚLTIMO tag: el último describe lo que está corriendo,
así que volver ahí no sería volver a ningún lado.

Verifica que la imagen exista ANTES de tocar el .env. Escribir un IMAGE_TAG
que apunta a una imagen inexistente deja el stack sin poder levantar y
convierte un rollback en una caída más larga.

Si no verifica en 90s no reintenta: imprime de dónde venía, qué intentó, el
último JSON y qué comandos correr. En ese punto lo único útil es que una
persona tenga los datos sin salir a buscarlos.

Lo va a invocar deploy.sh como su rollback automático, en vez de tener su
propia copia: así el camino de emergencia es el que se ejercita siempre.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 7: `deploy.sh` — esqueleto y preflight

El script arranca haciendo sólo los pasos 1 a 5 y terminando ahí. Es ejecutable y verificable desde esta tarea: las dos siguientes le agregan tramos.

**Files:**
- Create: `scripts/deploy.sh`

**Interfaces:**
- Consumes: `migracion_destructiva` de la Task 2.
- Produces: `scripts/deploy.sh [--minor] [--objetivo=prod|ensayo]`, con `flock`, `trap` y los pasos 1–5.

- [ ] **Step 1: Escribir el esqueleto**

Crear `scripts/deploy.sh`:

```bash
#!/usr/bin/env bash
# El gate de deploy. Ver docs/superpowers/specs/2026-08-06-deploy-design.md.
#
# Sin feature flags, este script, el healthcheck y el rollback son la ÚNICA red
# entre un error y todos los clientes a la vez. Un paso decorativo acá no es un
# gate incompleto: es un gate que miente.
#
# Este script ORQUESTA y no decide: toda la lógica que decide algo vive en
# scripts/lib/deploy-comun.sh, con unitarios que corren sin Docker.
set -euo pipefail
cd "$(dirname "${BASH_SOURCE[0]}")/.."
source scripts/lib/deploy-comun.sh

TIPO_VERSION=patch
OBJETIVO=prod

uso() {
  cat >&2 <<'EOF'
uso: deploy.sh [--minor] [--objetivo=prod|ensayo]

  --minor     sube MINOR en vez de PATCH. MINOR es para lo que el cliente ve
              (pantalla nueva, módulo, feature); PATCH para todo lo demás.
  --objetivo  prod (default) o ensayo. Con ensayo corre la secuencia completa
              contra el stack descartable, sin tagear ni pushear.
EOF
  exit 2
}

for arg in "$@"; do
  case "$arg" in
    --minor)      TIPO_VERSION=minor ;;
    --objetivo=*) OBJETIVO="${arg#*=}" ;;
    -h|--help)    uso ;;
    *) error "argumento desconocido: $arg"; uso ;;
  esac
done

case "$OBJETIVO" in
  prod)   DIR=/srv/arandano/prod;   URL_SALUD=http://127.0.0.1;            TAGEA=true ;;
  ensayo) DIR=/srv/arandano/ensayo; URL_SALUD=http://100.64.81.63:3002;    TAGEA=false ;;
  *) error "objetivo inválido: $OBJETIVO"; uso ;;
esac

# Un solo deploy a la vez. A diferencia de backup.sh, que ante un lock tomado se
# saltea la corrida y sale con 0, acá se ABORTA: saltearse un deploy en silencio
# y devolver éxito es cómo alguien termina creyendo que promovió algo que nunca
# promovió.
exec 9>/var/lock/arandano-deploy.lock
if ! flock -n 9; then
  error "ya hay un deploy corriendo"
  exit 1
fi

DEV_FRENADA=false

# Corre pase lo que pase, y preserva el código de salida original: si lo
# pisáramos con el del último comando de limpieza, un deploy fallido podría
# reportar éxito.
limpiar() {
  local codigo=$?
  set +e
  if docker ps --filter name=arandano-stage --format '{{.Names}}' | grep -q .; then
    log "bajando arandano-stage"
    docker compose -f docker/compose.stage.yml down -v >/dev/null 2>&1
  fi
  if [[ "$DEV_FRENADA" == true ]]; then
    log "volviendo a levantar arandano-dev"
    docker compose -f docker/compose.dev.yml up -d >/dev/null 2>&1
  fi
  exit "$codigo"
}
trap limpiar EXIT

SHA=$(git rev-parse --short HEAD)
log "deploy $SHA -> $OBJETIVO (versión: $TIPO_VERSION)"

# ---------------------------------------------------------------------------
# Preflight: nada tocado todavía. Cualquier falla acá aborta y no deja rastro.
# ---------------------------------------------------------------------------

# Paso 1. La imagen se tagea con el SHA, así que buildear con cambios sin
# commitear produce una imagen cuya etiqueta apunta a un código que NO contiene
# — y esa etiqueta es lo que alguien lee para saber qué está corriendo.
log "paso 1/16: working tree limpio"
if ! git diff --quiet || ! git diff --cached --quiet; then
  error "el working tree tiene cambios sin commitear"
  git status --short >&2
  exit 1
fi

# Paso 2. El mismo chequeo que el hook de pre-commit, repetido acá porque
# --no-verify existe.
log "paso 2/16: migraciones nuevas sin SQL destructivo"
ultimo_tag=$(git tag --list 'v1.*' --sort=-v:refname | head -1)
if [[ -n "$ultimo_tag" ]]; then
  migraciones_nuevas=$(git diff --name-only --diff-filter=ACM "$ultimo_tag..HEAD" \
                       -- 'prisma/migrations/**/migration.sql' || true)
else
  # Sin tags previos, todas las migraciones del repo son "nuevas".
  migraciones_nuevas=$(find prisma/migrations -name migration.sql | sort)
fi
if [[ -n "$migraciones_nuevas" ]]; then
  while IFS= read -r archivo; do
    if patron=$(migracion_destructiva "$(cat "$archivo")"); then
      error "migración destructiva en $archivo (patrón: $patron)"
      error "el rollback revierte la imagen, no la base: expand/contract es obligatorio"
      exit 1
    fi
  done <<< "$migraciones_nuevas"
  log "  $(wc -l <<< "$migraciones_nuevas") migración(es) nueva(s), todas aditivas"
else
  log "  sin migraciones nuevas"
fi

# Paso 3. Que schema.prisma no tenga cambios que ninguna migración capture.
# Alguien edita el modelo, se olvida de generar la migración y commitea; el
# deploy aplica cero migraciones y la app arranca contra un schema que no
# existe. Necesita una shadow database, que sale del mismo patrón efímero que
# usan verify-backup.sh y arandano-stage.
log "paso 3/16: schema.prisma y migraciones sincronizados"
SOMBRA=arandano-deploy-sombra
docker rm -f "$SOMBRA" >/dev/null 2>&1 || true
docker run -d --name "$SOMBRA" \
  --memory=512m --cpus=0.5 \
  --tmpfs /var/lib/postgresql/data:size=320m,mode=1777 \
  -e POSTGRES_USER=sombra -e POSTGRES_PASSWORD=sombra -e POSTGRES_DB=sombra \
  -e PGDATA=/var/lib/postgresql/data/pgdata \
  postgres:17-alpine >/dev/null

# NO alcanza el primer pg_isready: el entrypoint levanta un servidor TEMPORAL
# para los scripts de init, lo apaga, y recién ahí arranca el DEFINITIVO. La
# señal inequívoca es la SEGUNDA aparición de esta línea.
for _ in $(seq 60); do
  listo=$(docker logs "$SOMBRA" 2>&1 | grep -c 'database system is ready to accept connections' || true)
  [[ "$listo" -ge 2 ]] && break
  sleep 1
done
if [[ "${listo:-0}" -lt 2 ]]; then
  docker rm -f "$SOMBRA" >/dev/null 2>&1
  error "la shadow database no levantó en 60s"
  exit 1
fi

diff_rc=0
docker run --rm --network container:"$SOMBRA" \
  -v "$PWD/prisma:/app/prisma:ro" \
  -e MIGRATE_DATABASE_URL="postgres://sombra:sombra@127.0.0.1:5432/sombra" \
  "arandano-migrate:$SHA" migrate diff \
    --from-migrations prisma/migrations \
    --to-schema-datamodel prisma/schema.prisma \
    --shadow-database-url "postgres://sombra:sombra@127.0.0.1:5432/sombra" \
    --exit-code >/dev/null 2>&1 || diff_rc=$?
docker rm -f "$SOMBRA" >/dev/null 2>&1

# migrate diff --exit-code: 0 = sin diferencias, 2 = hay diferencias,
# 1 = el comando falló.
if [[ "$diff_rc" -eq 2 ]]; then
  error "schema.prisma tiene cambios que ninguna migración captura"
  error "correr: npx prisma migrate dev --name <descripcion>"
  exit 1
elif [[ "$diff_rc" -ne 0 ]]; then
  error "no se pudo comparar el schema contra las migraciones (exit $diff_rc)"
  exit 1
fi
log "  sin diferencias"

# Pasos 4 y 5.
log "paso 4/16: npm test"
npm test

log "paso 5/16: typecheck y lint"
npx tsc --noEmit
npm run lint

log "preflight ok"
```

```bash
chmod +x scripts/deploy.sh
```

**Nota sobre el orden de implementación:** el paso 3 usa `arandano-migrate:$SHA`, que se buildea en el paso 7. En un deploy real esa imagen todavía no existe para el SHA nuevo. La Task 8 mueve el paso 3 después del build; se escribe acá para poder probarlo con la imagen que ya existe (`25297f7`).

- [ ] **Step 2: Verificar que rechaza un objetivo inválido**

```bash
scripts/deploy.sh --objetivo=produccion; echo "exit: $?"
```
Expected: exit 2 con el uso.

- [ ] **Step 3: Verificar que el working tree sucio frena todo**

```bash
echo "# sucio" >> README.md
scripts/deploy.sh --objetivo=ensayo; echo "exit: $?"
git checkout README.md
```
Expected: exit 1 en el paso 1, con `git status --short` mostrando `README.md`. **No debe llegar al paso 2.**

- [ ] **Step 4: Verificar el lock**

```bash
( exec 9>/var/lock/arandano-deploy.lock; flock -n 9; sleep 20 ) &
sleep 1
scripts/deploy.sh --objetivo=ensayo; echo "exit: $?"
wait
```
Expected: exit 1 con `ya hay un deploy corriendo`.

- [ ] **Step 5: Correr el preflight completo**

```bash
scripts/deploy.sh --objetivo=ensayo
```
Expected: los cinco pasos en verde y `preflight ok`. El paso 3 levanta y tira la shadow database; el 4 corre la suite entera.

- [ ] **Step 6: Verificar que el paso 3 detecta un schema desincronizado**

```bash
printf '\nmodel Prueba {\n  id String @id\n}\n' >> prisma/schema.prisma
scripts/deploy.sh --objetivo=ensayo; echo "exit: $?"
git checkout prisma/schema.prisma
```
Expected: **falla en el paso 1**, porque el working tree quedó sucio. Commitear el cambio para probar el paso 3 de verdad:

```bash
printf '\nmodel Prueba {\n  id String @id\n}\n' >> prisma/schema.prisma
git add prisma/schema.prisma && git commit -q -m "temporal: schema sin migración"
scripts/deploy.sh --objetivo=ensayo; echo "exit: $?"
git reset --hard HEAD~1
```
Expected: exit 1 en el paso 3, con `schema.prisma tiene cambios que ninguna migración captura`. Este es el bloqueante 9 funcionando.

- [ ] **Step 7: Commit**

```bash
git add scripts/deploy.sh
git commit -m "$(cat <<'EOF'
feat(deploy): esqueleto del gate y su preflight

CLI, flock, trap de limpieza y los pasos 1 a 5: working tree limpio,
migraciones no destructivas, schema sincronizado contra las migraciones (con
shadow database efímera), tests y typecheck.

El lock ABORTA en vez de saltearse la corrida como hace backup.sh: saltearse
un deploy en silencio y salir con 0 es cómo alguien cree que promovió algo
que nunca promovió.

El trap preserva el código de salida original; pisarlo con el del último
comando de limpieza haría que un deploy fallido reporte éxito.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 8: `deploy.sh` — build y ensayo en stage

Los pasos 6 a 9. Acá el paso 3 se mueve después del build, porque necesita la imagen `arandano-migrate` del SHA nuevo.

**Files:**
- Modify: `scripts/deploy.sh`

**Interfaces:**
- Consumes: el esqueleto de la Task 7, `scripts/smoke.sh` de la Task 4, `scripts/setup-db-roles.sh --network`.
- Produces: `deploy.sh` corre hasta el paso 9 y termina.

- [ ] **Step 1: Mover el paso 3 después del build**

En `scripts/deploy.sh`, el bloque del chequeo de schema —desde el comentario
`# Paso 3.` hasta la línea `log "  sin diferencias"` inclusive— se **mueve** sin
tocar su contenido: sale de donde está y entra inmediatamente después del
segundo `docker build` que agrega el Step 2 de esta tarea. Necesita la imagen
`arandano-migrate:$SHA`, que antes del build no existe para el SHA nuevo.

Los `log "paso N/16"` se renumeran para que el número siga al orden real. La
numeración final queda así:

| Log | Qué |
|---|---|
| `paso 1/16` | working tree limpio |
| `paso 2/16` | migraciones no destructivas |
| `paso 3/16` | `npm test` |
| `paso 4/16` | typecheck y lint |
| `paso 5/16` | frenar `arandano-dev` |
| `paso 6/16` | buildear las dos imágenes |
| `paso 7/16` | schema sincronizado (shadow database) |
| `paso 8/16` | stage: roles y `migrate deploy` sobre base virgen |
| `paso 9/16` | smoke tests contra stage |

- [ ] **Step 2: Agregar los pasos 5 y 6 (frenar dev y buildear)**

Después del bloque de typecheck y lint, agregar:

```bash
# ---------------------------------------------------------------------------
# Build
# ---------------------------------------------------------------------------

# Paso 5. ANTES del build, no antes de stage. La aritmética no cierra de otra
# forma: prod 3200 + dev 2304 + build 2048 + ~1,1 GB de sistema ≈ 8,5 GB sobre
# una caja de 7,6 GB. Con dev abajo desde acá el pico queda en ~7,5 GB. De paso
# queda cubierta la regla de que dev y stage no corren juntos.
log "paso 5/16: frenando arandano-dev"
docker compose -f docker/compose.dev.yml down
DEV_FRENADA=true

# Paso 6. --target explícito en las dos: `docker build` sin --target buildea la
# ÚLTIMA etapa del archivo, y el día que alguien agregue una al final,
# arandano-app pasaría a contener otra cosa sin que nada avise. Las banderas de
# recursos son las que efectivamente limitan en este host; nice, --cpuset-cpus
# y --memory son inertes acá y no avisan que lo son.
log "paso 6/16: buildeando arandano-app:$SHA y arandano-migrate:$SHA"
docker build --cgroup-parent=arandanobuild.slice \
  --resource memory=2g --resource cpu-quota=100000 \
  --target runtime --build-arg GIT_SHA="$SHA" -t "arandano-app:$SHA" .
docker build --cgroup-parent=arandanobuild.slice \
  --resource memory=2g --resource cpu-quota=100000 \
  --target migrate --build-arg GIT_SHA="$SHA" -t "arandano-migrate:$SHA" .
```

- [ ] **Step 3: Agregar los pasos 8 y 9 (stage)**

Después del bloque del schema (ahora paso 7), agregar:

```bash
# ---------------------------------------------------------------------------
# Ensayo en stage: la migración se prueba sobre una base VIRGEN antes de tocar
# la de clientes. Si va a explotar, que explote acá.
# ---------------------------------------------------------------------------

log "paso 8/16: levantando arandano-stage y ensayando la migración"
export IMAGE_TAG="$SHA"
docker compose -f docker/compose.stage.yml up -d --wait postgres

# Los roles de stage no existen: la base nace vacía en cada corrida. Las
# contraseñas están en claro en compose.stage.yml y eso es aceptable sólo acá,
# porque la base es efímera y nunca ve datos de clientes.
scripts/setup-db-roles.sh \
  --network=arandano-stage_default \
  --url="postgres://arandano_stage:efimero-no-persiste@postgres:5432/arandano_stage" \
  --owner-password=efimero-owner \
  --app-password=efimero-app \
  --con-createdb

docker run --rm --network arandano-stage_default \
  -e MIGRATE_DATABASE_URL="postgres://arandano_owner:efimero-owner@postgres:5432/arandano_stage" \
  "arandano-migrate:$SHA" migrate deploy

# --wait espera al healthcheck del compose, no a que el contenedor arranque:
# sin eso el smoke test correría contra un Next todavía levantando, y las
# fallas intermitentes del gate se leen como bugs del código.
docker compose -f docker/compose.stage.yml up -d --wait app

log "paso 9/16: smoke tests contra stage"
scripts/smoke.sh http://100.64.81.63:3001 "$SHA"

docker compose -f docker/compose.stage.yml down -v
log "ensayo en stage ok"
```

- [ ] **Step 4: Correr hasta el paso 9**

```bash
scripts/deploy.sh --objetivo=ensayo
```
Expected: los nueve pasos en verde, terminando en `ensayo en stage ok`. Toma varios minutos: el build es lo más lento.

Al terminar, el `trap` tiene que haber vuelto a levantar dev:

```bash
docker ps --filter name=arandano-dev --format '{{.Names}}'
```
Expected: los dos contenedores de dev arriba.

- [ ] **Step 5: Verificar que una falla del smoke test frena el deploy**

```bash
sed -i 's|scripts/smoke.sh http://100.64.81.63:3001 "$SHA"|scripts/smoke.sh http://100.64.81.63:3001 0000000|' scripts/deploy.sh
scripts/deploy.sh --objetivo=ensayo; echo "exit: $?"
git checkout scripts/deploy.sh
```
Expected: exit distinto de 0 en el paso 9, con `caso_sha_esperado` en rojo. Y el `trap` igual bajó stage y levantó dev:

```bash
docker ps --filter name=arandano-stage --format '{{.Names}}'
docker ps --filter name=arandano-dev --format '{{.Names}}'
```
Expected: stage sin salida, dev con sus dos contenedores.

- [ ] **Step 6: Commit**

```bash
git add scripts/deploy.sh
git commit -m "$(cat <<'EOF'
feat(deploy): build y ensayo de la migración en stage

Frena dev antes del build (no antes de stage): es lo único que hace cerrar la
aritmética de memoria. Buildea las dos imágenes con --target explícito.

El ensayo en stage va ANTES de tocar producción, corrigiendo el orden que
documentaba CLAUDE.md: la migración se prueba sobre una base virgen antes que
sobre la de clientes. Si va a explotar, explota acá.

El chequeo de schema se mueve después del build porque necesita la imagen
arandano-migrate del SHA nuevo.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 9: `deploy.sh` — producción, promoción y rollback

Los pasos 10 a 16. Es el tramo donde el fallo deja de abortar y pasa a rollbackear.

**Files:**
- Modify: `scripts/deploy.sh`

**Interfaces:**
- Consumes: `scripts/rollback.sh` de la Task 6, `scripts/backup.sh --motivo=pre-migracion`, `proxima_version` y `mensaje_de_tag` de la Task 2.
- Produces: `deploy.sh` completo.

- [ ] **Step 1: Agregar los pasos 10 a 12**

Al final de `scripts/deploy.sh`:

```bash
# ---------------------------------------------------------------------------
# Producción. De acá en adelante el objetivo se toca de verdad.
# ---------------------------------------------------------------------------

set -a; . "$DIR/.env"; set +a

# Paso 10. Que las migraciones del repo sean exactamente las aplicadas en el
# objetivo. Esto no puede vivir en un hook de pre-commit: preguntárselo a
# producción exige apuntar herramientas de desarrollo a su base con sus
# credenciales, que es justo la separación que este proyecto construye.
log "paso 10/16: migraciones del repo == migraciones del objetivo"
RED_OBJETIVO="arandano-${OBJETIVO}_default"
docker run --rm --network "$RED_OBJETIVO" \
  -e MIGRATE_DATABASE_URL="$MIGRATE_DATABASE_URL" \
  "arandano-migrate:$SHA" migrate status

# Paso 11. Contrato del spec de backups: si el backup falla, se aborta ANTES de
# migrar. Una migración sin backup previo es el escenario irreversible que esos
# scripts existen para evitar.
log "paso 11/16: backup pre-migración"
if [[ "$OBJETIVO" == prod ]]; then
  scripts/backup.sh --motivo=pre-migracion
else
  # En ensayo el backup igual CORRE —para que el paso se ejercite— pero contra
  # el prefijo test/ del bucket, para no ensuciar el histórico real de prod.
  scripts/backup.sh --motivo=test
fi

# Paso 12. Sólo migrate deploy. Nunca migrate reset ni db push.
log "paso 12/16: migrate deploy contra $OBJETIVO"
docker run --rm --network "$RED_OBJETIVO" \
  -e MIGRATE_DATABASE_URL="$MIGRATE_DATABASE_URL" \
  "arandano-migrate:$SHA" migrate deploy
```

- [ ] **Step 2: Agregar los pasos 13 y 14 con el rollback**

```bash
# ---------------------------------------------------------------------------
# Desde acá, una falla es ROLLBACK y no aborto: es el único tramo donde el
# objetivo ya cambió. Un aborto en el paso 12 deja schema nuevo con código
# viejo, que es el estado que expand/contract garantiza compatible.
# ---------------------------------------------------------------------------

TAG_ANTERIOR=$(grep -oP '^IMAGE_TAG=\K.*' "$DIR/.env")

# El rollback automático NO reimplementa nada: invoca el mismo comando que
# correría una persona. Así el camino de emergencia es el que se ejercita.
rollback_y_salir() {
  error "$1"
  error "disparando rollback automático a $TAG_ANTERIOR"
  if scripts/rollback.sh --a="$TAG_ANTERIOR" --objetivo="$OBJETIVO"; then
    error "rollback ok: $OBJETIVO volvió a $TAG_ANTERIOR. NO se creó tag."
  else
    error "EL ROLLBACK TAMBIÉN FALLÓ. Ver el detalle de arriba."
  fi
  exit 1
}

log "paso 13/16: promoviendo arandano-app:$SHA (venía de $TAG_ANTERIOR)"
sed -i "s|^IMAGE_TAG=.*|IMAGE_TAG=${SHA}|" "$DIR/.env"
if ! ( cd "$DIR" && docker compose up -d --force-recreate app ); then
  rollback_y_salir "la promoción falló"
fi

# Paso 14. Con plazo, no con reintentos infinitos: un healthcheck que espera
# para siempre no es un gate.
#
# La comparación de sha es el bloqueante 8: el healthcheck puede dar 200 desde
# el contenedor VIEJO si la promoción no reemplazó nada, y el deploy se
# declararía exitoso aunque lo que respondió nunca haya sido lo que pasó el
# smoke test.
log "paso 14/16: healthcheck de $OBJETIVO (plazo 90s)"
limite=$((SECONDS + 90))
salud=""
sano=false
while (( SECONDS < limite )); do
  salud=$(curl -fsS --max-time 5 "$URL_SALUD/api/health" 2>/dev/null) || salud=""
  if [[ -n "$salud" ]] && health_ok "$salud" \
     && [[ "$(sha_del_health "$salud")" == "$SHA" ]]; then
    sano=true
    break
  fi
  sleep 3
done
if [[ "$sano" != true ]]; then
  rollback_y_salir "el healthcheck no dio sano con sha=$SHA en 90s (último: ${salud:-<sin respuesta>})"
fi
log "  $OBJETIVO responde sano con sha=$SHA"
```

- [ ] **Step 3: Agregar los pasos 15 y 16**

```bash
# ---------------------------------------------------------------------------
# Paso 15. El tag va DESPUÉS del healthcheck: significa "esto estuvo vivo y sano
# en producción", no "esto se intentó". Un deploy que rollbackea no consume
# número, así que la secuencia no tiene huecos.
#
# Una falla acá NO dispara rollback. Si el push falla —se cayó la red, venció el
# token— producción ya está viva y sana; revertirla por un problema de metadatos
# convertiría un fallo cosmético en una caída.
# ---------------------------------------------------------------------------

if [[ "$TAGEA" == true ]]; then
  log "paso 15/16: tag de git"
  VERSION=$(proxima_version "$ultimo_tag" "$TIPO_VERSION")
  nombres_migraciones=$(printf '%s\n' "$migraciones_nuevas" \
    | sed -nE 's|^prisma/migrations/([^/]+)/migration\.sql$|\1|p' | paste -sd' ' -)
  if git tag -a "$VERSION" -m "$(mensaje_de_tag "$SHA" "$nombres_migraciones")"; then
    log "  $VERSION creado"
    if git push origin "$VERSION"; then
      log "  $VERSION pusheado a origin"
    else
      error "el tag $VERSION se creó pero NO se pudo pushear"
      error "producción está sana; esto es metadatos. Reintentar: git push origin $VERSION"
    fi
  else
    error "no se pudo crear el tag $VERSION. Producción está sana."
  fi
else
  log "paso 15/16: objetivo=$OBJETIVO, no se tagea"
fi

log "paso 16/16: el trap vuelve a levantar arandano-dev"
log "deploy ok: $OBJETIVO corriendo arandano-app:$SHA"
```

- [ ] **Step 4: Verificar la secuencia completa contra ensayo**

Levantar el stack de ensayo primero, porque el paso 10 necesita su Postgres:

```bash
cd /srv/arandano/ensayo && docker compose up -d postgres && cd /root/arandano
scripts/deploy.sh --objetivo=ensayo
```
Expected: falla en el paso 10 con `migrate status`, porque el ensayo todavía no tiene roles ni las dos URLs en su `.env`. **Eso es correcto**: la Task 10 arma esa preparación. Anotarlo y seguir.

- [ ] **Step 5: Commit**

```bash
git add scripts/deploy.sh
git commit -m "$(cat <<'EOF'
feat(deploy): la zona de producción, con promoción y rollback

Pasos 10 a 16: migrate status contra el objetivo, backup pre-migración,
migrate deploy, promoción, healthcheck con plazo y comparación de sha, tag
anotado y push.

El rollback automático invoca scripts/rollback.sh en vez de reimplementarlo:
así el camino de emergencia es exactamente el que se ejercita en cada deploy.

Tres zonas de fallo distintas. Hasta el paso 12 se aborta y no hay nada que
revertir. En 13 y 14 se rollbackea. En el 15 se avisa y NO se rollbackea: si
falla el push del tag, producción ya está sana y revertirla por metadatos
convertiría un fallo cosmético en una caída.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 10: El ensayo completo

Preparar el stack de ensayo para que la secuencia corra de punta a punta, y correrla. Es la única forma de ejercitar la coreografía — los unitarios cubren cada decisión, pero el orden de los pasos no lo prueba ningún test unitario.

**Files:**
- Modify: `scripts/deploy.sh` (lo que el ensayo encuentre)

**Interfaces:**
- Consumes: todo lo anterior.
- Produces: la confianza para apuntarlo a producción.

- [ ] **Step 1: Preparar el stack de ensayo como si fuera un alta de prod**

```bash
cd /srv/arandano/ensayo && docker compose up -d postgres && cd /root/arandano
set -a; . /srv/arandano/ensayo/.env; set +a

OWNER_PASS=$(openssl rand -hex 24)
APP_PASS=$(openssl rand -hex 24)

scripts/setup-db-roles.sh \
  --network=arandano-ensayo_default \
  --url="postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}" \
  --owner-password="$OWNER_PASS" \
  --app-password="$APP_PASS"

{
  grep -v -E '^(DATABASE_URL|MIGRATE_DATABASE_URL)=' /srv/arandano/ensayo/.env
  echo "DATABASE_URL=postgres://arandano_app:${APP_PASS}@postgres:5432/${POSTGRES_DB}"
  echo "MIGRATE_DATABASE_URL=postgres://arandano_owner:${OWNER_PASS}@postgres:5432/${POSTGRES_DB}"
} > /srv/arandano/ensayo/.env.nuevo
chmod 600 /srv/arandano/ensayo/.env.nuevo
mv /srv/arandano/ensayo/.env.nuevo /srv/arandano/ensayo/.env
grep -oP '^[A-Z_]+(?==)' /srv/arandano/ensayo/.env
```
Expected: las seis claves, sin imprimir ningún valor.

**Ojo:** el Postgres de ensayo es tmpfs, así que si el contenedor se baja, los roles se pierden y este paso hay que repetirlo. Es la naturaleza del stack, no un bug.

- [ ] **Step 2: Correr el ensayo completo**

```bash
scripts/deploy.sh --objetivo=ensayo
```
Expected: los 16 pasos en verde, terminando en `deploy ok: ensayo corriendo arandano-app:<sha>`. Sin tag creado:

```bash
git tag --list 'v1.*'
```
Expected: sin salida.

- [ ] **Step 3: Verificar el estado final del ensayo**

```bash
curl -s http://100.64.81.63:3002/api/health | jq .
grep '^IMAGE_TAG=' /srv/arandano/ensayo/.env
docker ps --filter name=arandano-dev --format '{{.Names}}'
docker ps --filter name=arandano-stage --format '{{.Names}}'
```
Expected: healthcheck sano con el sha nuevo, `IMAGE_TAG` actualizado, dev arriba, stage abajo.

- [ ] **Step 4: Probar el rollback automático de verdad**

La prueba que más importa: que el rollback se dispare y funcione. Se fuerza rompiendo el healthcheck del paso 14 con un sha imposible.

El paso 1 del gate exige working tree limpio, así que el cambio temporal tiene
que ir commiteado. `git reset --hard HEAD~1` al final lo borra y deja el árbol
como estaba — no hace falta ninguna copia de respaldo.

```bash
sed -i 's|== "$SHA" \]\]|== "imposible" ]]|' scripts/deploy.sh
git add scripts/deploy.sh && git commit -q -m "temporal: forzar fallo del healthcheck"

ANTES=$(grep -oP '^IMAGE_TAG=\K.*' /srv/arandano/ensayo/.env)
scripts/deploy.sh --objetivo=ensayo; echo "exit: $?"
DESPUES=$(grep -oP '^IMAGE_TAG=\K.*' /srv/arandano/ensayo/.env)
echo "antes=$ANTES despues=$DESPUES"

git reset --hard HEAD~1
git diff --quiet && echo "árbol limpio, el cambio temporal se fue"
```

El `sed` sólo debe tocar **una** línea (la del paso 14). Verificarlo antes de
commitear con `git diff --stat`: si cambió más de una, el patrón es demasiado
amplio y el ensayo estaría probando otra cosa.
Expected: el deploy falla en el paso 14, dispara el rollback, y `DESPUES` es igual a `ANTES`. El healthcheck de ensayo tiene que responder sano con el sha viejo:

```bash
curl -s http://100.64.81.63:3002/api/health | jq -r '.info.sha'
```
Expected: el sha anterior. **Si el rollback no volvió, parar acá**: es el mecanismo entero.

- [ ] **Step 5: Arreglar lo que el ensayo haya encontrado**

Todo lo que falle en los pasos 2 a 4 se arregla ahora, con su commit propio y un mensaje que diga qué encontró el ensayo. Volver a correr el Step 2 hasta que pase de punta a punta.

- [ ] **Step 6: Bajar el stack de ensayo**

```bash
cd /srv/arandano/ensayo && docker compose down -v && cd /root/arandano
docker ps --filter name=arandano-ensayo --format '{{.Names}}'
```
Expected: sin salida.

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "$(cat <<'EOF'
test(deploy): la secuencia completa ensayada contra el stack descartable

Los 16 pasos corridos de punta a punta contra arandano-ensayo, incluido el
rollback automático forzado a dispararse: la promoción vuelve sola a la imagen
anterior y el healthcheck lo confirma.

Es lo único que ejercita la coreografía. Los unitarios cubren cada decisión
por separado; el ORDEN de los pasos, que es donde vive el riesgo de un deploy,
no lo prueba ningún test unitario.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 11: Documentación

**Files:**
- Modify: `CLAUDE.md`
- Modify: `docs/runbook-stacks.md`

**Interfaces:**
- Consumes: todo lo anterior.
- Produces: el documento maestro sin contradicciones.

- [ ] **Step 1: Corregir dónde corren los smoke tests**

En `CLAUDE.md`, la línea que dice que los smoke tests corren contra `arandano-dev` contradice a otras tres y es imposible (dev está abajo durante todo el deploy). Reemplazarla por:

```markdown
**3. Los smoke tests son la última verificación antes de que llegue a todos.** Corren contra `arandano-stage` con la imagen ya buildeada, sobre una base efímera en tmpfs que arranca vacía —así el ensayo de la migración significa algo—, y sobre los caminos que más duelen. Hoy son los que existen: healthcheck sano de verdad (todos sus checks, no sólo el status), el sha reportado igual al promovido, el rol de conexión sin privilegios y la home respondiendo. Cuando existan login, alta de venta, emisión de factura contra homologación, apertura y cierre de orden de trabajo y catálogo público, sus casos entran en `scripts/smoke.sh`.
```

- [ ] **Step 2: Corregir cuándo arranca la numeración**

Reemplazar `arrancando en v1.0.0 con el primer deploy que sirva a un tenant real` por:

```markdown
**Formato `v1.MINOR.PATCH`**, arrancando en `v1.0.0` con el primer deploy que pase el gate completo, haya tenants o no. La alternativa —numerar recién con el primer tenant— dejaba al rollback manual con dos modos según la época, y el modo sin tags, que es el único disponible justo al principio, sería el que nunca se ejercita después.
```

- [ ] **Step 3: Corregir el orden documentado del gate**

Reemplazar la línea que empieza `**El deploy es un comando con gate.**` por:

```markdown
**El deploy es un comando con gate.** `deploy.sh` encadena: working tree limpio → migraciones no destructivas → tests → typecheck → frenar `arandano-dev` → build tageado → schema sincronizado con las migraciones → ensayo de la migración y smoke tests contra `arandano-stage` → `migrate status` contra prod → backup → `migrate deploy` → promoción de la imagen → healthcheck con comparación de SHA → tag de git pusheado a `origin`, con rollback automático a la imagen anterior si falla la promoción o el healthcheck. El ensayo en stage va **antes** de tocar producción: la migración se prueba sobre una base virgen antes que sobre la de clientes. Sin pasos manuales que se puedan saltear un martes a las 11 de la noche.
```

- [ ] **Step 4: Cerrar los bloqueantes**

En *Bloqueantes antes del primer tenant real*, reemplazar los puntos 3 a 10 por uno solo:

```markdown
3. ~~**`deploy.sh`** con su gate completo, incluidos el rechazo del working tree
   sucio, `GIT_SHA` explícito, frenar dev antes del build, el presupuesto de
   recursos del build, la comparación de `info.sha`, la verificación de
   migraciones en las dos direcciones, y el rechazo de migraciones
   destructivas.~~ **Hecho** (2026-08-06). Ver
   `docs/superpowers/specs/2026-08-06-deploy-design.md` y la sección *Deploy y
   rollback* de `docs/runbook-stacks.md`.
```

- [ ] **Step 5: Sumar la sección de deploy al runbook**

En `docs/runbook-stacks.md`, agregar antes de *Certificado de producción, hoy*:

```markdown
## Deploy y rollback

```bash
scripts/deploy.sh              # patch: fixes, refactors, migraciones aditivas
scripts/deploy.sh --minor      # minor: algo que el cliente ve
scripts/deploy.sh --objetivo=ensayo   # la secuencia completa, sin tocar prod
```

El script frena `arandano-dev` al arrancar y lo vuelve a levantar al terminar,
pase lo que pase. Toma un `flock`: si ya hay un deploy corriendo, **aborta** en
vez de saltearse la corrida — salir con 0 sin haber promovido es cómo alguien
cree que deployó algo que nunca deployó.

**Tres zonas de fallo.** Hasta `migrate deploy` inclusive, una falla aborta y no
hay nada que revertir: producción sigue con su imagen anterior, y si la
migración llegó a correr, el schema nuevo con el código viejo es un estado
compatible por construcción (expand/contract). Desde la promoción, una falla
dispara el rollback automático. La creación del tag es la excepción: si falla,
producción ya está sana y **no** se rollbackea.

**Rollback a mano**, para lo que el healthcheck no ve:

```bash
scripts/rollback.sh            # al anteúltimo tag
scripts/rollback.sh --a=<sha>  # a una imagen concreta
```

No toca la base de datos. Nunca. Por eso el gate se niega ante una migración
destructiva: si el schema nuevo no soporta el código viejo, revertir la imagen
no alcanza.

**El hook de pre-commit** frena una migración destructiva antes de que entre al
repo. Se activa con `git config core.hooksPath .githooks`, que ya está en
`scripts/setup-host.sh`. Es una red temprana, no la definitiva: `deploy.sh`
vuelve a chequear lo mismo porque `--no-verify` existe.
```

- [ ] **Step 6: Verificar que no quedaron contradicciones**

```bash
grep -n -i "smoke" CLAUDE.md
grep -n "tenant real" CLAUDE.md | head -5
```
Expected: ninguna mención de smoke tests contra `arandano-dev`, y ninguna que ate el `v1.0.0` al primer tenant.

- [ ] **Step 7: Commit**

```bash
git add CLAUDE.md docs/runbook-stacks.md
git commit -m "$(cat <<'EOF'
docs: deploy.sh cerrado, y CLAUDE.md sin sus contradicciones

Los smoke tests corren contra arandano-stage. La línea que decía arandano-dev
contradecía a otras tres y era además imposible: dev está abajo durante todo
el deploy.

La numeración arranca con el primer deploy que pase el gate, no con el primer
tenant, porque el rollback manual depende de que haya tags.

El orden documentado del gate ahora refleja el real, con el ensayo en stage
antes de tocar producción.

Cierra los bloqueantes 3 a 10 en uno solo.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 12: El primer deploy real

`v1.0.0`. Producción hoy corre `25297f7`, promovido a mano; este es el primero que pasa por el gate.

**Files:** ninguno del repo.

**Interfaces:**
- Consumes: todo lo anterior.
- Produces: producción deployada por el gate, con su tag.

- [ ] **Step 1: Mirar el radio de daño**

```bash
git log --oneline 25297f7..HEAD
git diff --stat 25297f7..HEAD -- app lib prisma
```
Expected: los cambios son scripts, docs y config. **Ninguno toca `app/`, `lib/` ni `prisma/`**, así que la imagen nueva es funcionalmente idéntica a la que ya corre. Es el primer deploy más barato posible: si algo falla, falla el mecanismo, no el producto.

- [ ] **Step 2: Confirmar la ventana**

Los clientes son comercios argentinos (UTC-3) que trabajan de 9 a 20. Hoy no hay tenants, así que la ventana no aplica todavía — pero conviene correrlo igual en horario de deploy, para que el hábito exista antes de que importe.

- [ ] **Step 3: Correr el deploy**

```bash
scripts/deploy.sh
```
Expected: los 16 pasos en verde, `v1.0.0` creado y pusheado.

- [ ] **Step 4: Verificar producción**

```bash
curl -s http://127.0.0.1/api/health | jq .
git tag -n99 v1.0.0
git ls-remote --tags origin | grep v1.0.0
```
Expected: healthcheck sano con el sha nuevo; el tag con su mensaje de imagen y migraciones; y el tag presente en `origin` — un tag que sólo existe en el VPS desaparece con el VPS, que es justo el escenario donde más se necesita.

- [ ] **Step 5: Verificar que el rollback tiene a dónde volver**

```bash
scripts/rollback.sh; echo "exit: $?"
```
Expected: exit 1 con `hacen falta al menos dos` tags. Es correcto: `v1.0.0` es el único. A partir de `v1.0.1` el rollback manual funciona sin argumentos.

- [ ] **Step 6: Correr la verificación de infraestructura**

```bash
scripts/verify-infra.sh
```
Expected: todas las suites en verde.

---

## Verificación final

- [ ] `npm test` — los tests de bash y los de vitest, todo en verde.
- [ ] `npx tsc --noEmit` y `npm run lint` — sin errores.
- [ ] `scripts/verify-infra.sh` — todas las suites en verde.
- [ ] `scripts/deploy.sh --objetivo=ensayo` — los 16 pasos, sin tocar producción.
- [ ] `curl -s http://127.0.0.1/api/health` — sano, con el sha del último deploy.
- [ ] `git tag -n99` — `v1.0.0` con su mensaje completo, presente también en `origin`.
- [ ] `git log --oneline` — un commit por tarea, ninguno con el árbol sucio detrás.
